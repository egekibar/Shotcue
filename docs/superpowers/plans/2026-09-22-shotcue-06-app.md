# Shotcue v1 — Plan 06: App (composition root, paneller, yaşam döngüsü)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Başlama koşulu:** Bu plan, Plan 01–05 tamamlanıp `swift build` yeşil olduktan sonra başlar. Plan 06 hiçbir yeni protokol veya domain tipi üretmez; yalnızca Plan 00–05'in ürettiği somut tipleri birbirine bağlar, AppKit pencerelerini kurar ve uygulama yaşam döngüsünü yönetir.

**Goal:** `Sources/ShotcueApp` içinde Shotcue'nun composition root'unu (tüm somut servislerin tek yerde üretimi), SwiftUI sahnelerini (`MenuBarExtra` + `Window("Kütüphane")` + `Settings`), odak çalmayan hızlı paneli, onboarding penceresini, bildirim aksiyonlarını, global kısayol → yakalama akışını, login item'ı, tanılamayı ve uygulama ikonunu kurmak; uçtan uca akışı gerçek `claude` ile elle doğrulamak.

**Architecture:** `AppEnvironment` (tek `@MainActor` sınıf) `SettingsStore` değerlerinden `FileStore` → `AppDatabase` → repository'ler → capture/notes/claude servisleri → `RunCoordinator` → `SchedulerDriver` → `AppServices` zincirini kurar ve Plan 05 store'larını üretir. `AppDelegate` yalnızca yaşam döngüsü sürücüsüdür (kısayol kaydı, bildirim delegesi, kurtarma, zamanlayıcı başlatma). AppKit köprüleri üç ince sınıfta izole: `QuickPanelController` (`NSPanel`), `OnboardingWindowController` (`NSWindow`), `AppWindowCoordinator` (`openWindow` köprüsü + aktivasyon politikası). App target'ında test hedefi yoktur; bu yüzden App kodu **yalnızca kablolama** içerir, karar mantığı ya Core'un saf fonksiyonlarına ya da `AppSettingsBridge`'in tek saf eşleme fonksiyonuna toplanır ve doğrulama `make build` + her task'ın sonundaki somut `make run` / `make shot` gözlemleriyle yapılır.

**Tech Stack:** Swift 6.4 (CLT 27.0, SDK 27.0), SwiftUI (`MenuBarExtra`, `Window`, `Settings`, `@NSApplicationDelegateAdaptor`, `@Environment(\.openWindow)`), AppKit (`NSPanel`, `NSHostingView`, `NSScreen`, `NSEvent` local monitor, `NSWorkspace.openApplication`), `ServiceManagement` (`SMAppService.mainApp`), `UserNotifications` (kategoriler + aksiyonlar), CoreGraphics + ImageIO (ikon üretimi), `iconutil` + `sips`.

**Spec:** `docs/superpowers/specs/2026-09-22-shotcue-design.md` (bu planın kapsamı: §5 tüm akışlar, §6.6 NSPanel/Dock, §6.7 App, §8 hata yönetimi, §9 izinler, §10 build, §11 manuel kontrol listesi, §12 spike'lar) · Araştırma: `docs/research/01` §4/§8, `docs/research/02` §4/§6, `docs/research/04` §6

## Global Constraints

- `// swift-tools-version: 6.4`, `platforms: [.macOS(.v26)]`, `swiftLanguageModes: [.v6]`.
- UI/App target'ları `.defaultIsolation(MainActor.self)`; servis/test target'ları `.defaultIsolation(nil)`; tüm target'larda `NonisolatedNonsendingByDefault` ve `InferIsolatedConformances` upcoming feature'ları.
- **Xcode yok.** `xcodebuild`, `actool`, `.xcassets`, `#Preview` makrosu ve `#Preview` içeren bağımlılıklar **yasak** (CLT'de `PreviewsMacros` eklentisi yok, derleme kırılır). Yalnızca `swift build`, `swift test`, `swift-format`, `codesign`, `iconutil`, `plutil`.
- Bağımlılık: yalnızca `https://github.com/groue/GRDB.swift.git` `from: "7.11.0"` ve `https://github.com/argmaxinc/argmax-oss-swift.git` `from: "1.1.0"`. Başka paket eklenmez.
- Bundle id `com.shotcue.app`; `LSUIElement` true; `LSMinimumSystemVersion` 26.0; depolama kökü `~/Library/Application Support/Shotcue/`.
- İmza: self-signed "Shotcue Dev" (ad-hoc `--sign -` yalnızca sertifika yoksa uyarıyla); App Sandbox **kapalı**; Hardened Runtime **açık**; entitlement `com.apple.security.device.audio-input = true`.
- Kod, identifier'lar ve commit mesajları İngilizce; UI metinleri Türkçe literal; Swift Testing (`import Testing`, `#expect`, `#require`); XCTest kullanılmaz.
- Domain görev tipi **`ShotTask`** (Swift'in `Task` tipiyle çakışmasın diye). Çalışma kaydı `Run`, ekran görüntüsü `Capture`, ses notu `VoiceNote`, proje `Project`.
- `ShotcueCore` yalnızca `Foundation` import eder. AppKit/SwiftUI/AVFoundation/GRDB Core'a giremez.
- Her task `swift test` yeşilken commit'lenir. Commit mesajı sonu: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

## Plan 06'ya özel ek kurallar

- **`ShotcueApp` için test target'ı yoktur ve `Package.swift` donmuştur** (yalnızca Plan 00 yazar). Bu yüzden App kodu ince kalır: her task'ın doğrulaması `make build` (derleme kanıtı) + `make run`/`make shot` ile **somut, yazılı gözlem** üzerinden yapılır. "Çalışıyor gibi görünüyor" kabul edilmez; her adımda hangi satırın/görüntünün beklendiği yazılıdır.
- App target'ı yalnızca şu dosyalara dokunur: `Sources/ShotcueApp/**`, `Resources/AppIcon.icns`, `scripts/make-icon.sh`, `scripts/render-icon.swift`, `docs/superpowers/plans/verification/**`. `Package.swift`, `Makefile`, `Resources/Info.plist`, `Resources/Shotcue.entitlements`, diğer `Sources/*` ve `Tests/*` **değiştirilmez**.
- `#Preview` yok, `.xcassets` yok. Hızlı panelden **asla** `NSApp.activate(ignoringOtherApps:)` çağrılmaz (araştırma 02 §6 tuzak 2).
- `swift-format` PATH'te değil; CLT içinde: `xcrun swift-format`. Bu planın format adımları `xcrun swift-format format --in-place --recursive Sources/ShotcueApp` kullanır (`make format` çalışıyorsa o da olur).

## Doğrulanmış platform bulguları (2026-09-22, scratchpad `spike-app` paketinde derlenip çalıştırıldı)

Bu planda geçen her AppKit/SwiftUI/ServiceManagement/UserNotifications çağrısı, Plan 06 ile aynı manifest ayarlarıyla (`swift-tools-version 6.4`, `platforms: [.macOS(.v26)]`, `swiftLanguageModes: [.v6]`, `.defaultIsolation(MainActor.self)`, `NonisolatedNonsendingByDefault`, `InferIsolatedConformances`) kurulmuş bir executable target'ta derlendi; pencere/panel davranışları ad-hoc imzalı gerçek bir `.app` bundle'ı çalıştırılarak ölçüldü.

1. **`@State` CLT'de derlenmiyor — `#Preview` ile aynı sınıf sorun.** macOS 27 SDK'da `SwiftUICore`, `State`'i property wrapper struct'ının **yanı sıra** bir attached macro olarak da tanımlıyor (`@attached(accessor) public macro State() = #externalMacro(module: "SwiftUIMacros", type: "StateMacro")`). Attribute pozisyonunda makro kazanıyor ve CLT'nin plugin dizininde (`/Library/Developer/CommandLineTools/usr/lib/swift/host/plugins/` → yalnızca `libObservationMacros.dylib`, `libSwiftMacros.dylib`, `testing`) `SwiftUIMacros` yok:
   ```
   error: external macro implementation type 'SwiftUIMacros.StateMacro' could not be found
          for macro 'State()'; plugin for module 'SwiftUIMacros' not found
   ```
   `@State var x = ""`, `@State var x: String = ""`, `@SwiftUICore.State var x: String = ""` ve `init` içinde `_x = State(initialValue:)` varyantlarının **hepsi** bu hatayı veriyor.
   **Çözüm (ölçülerek doğrulandı):** property wrapper struct'ına typealias açmak makro yerine wrapper'ı seçtiriyor:
   ```swift
   typealias UIState<Value> = SwiftUI.State<Value>   // @UIState private var note: String = ""  → derleniyor
   ```
   `@FocusState`, `@Binding`, `@Environment`, `@Bindable`, `@AppStorage`, `@Namespace`, `@StateObject`, `@ObservedObject`, `@Observable` **sorunsuz derleniyor** (yalnızca `State` makrolaşmış). Plan 06'nın kendi görünümleri zaten `@Observable` store'lara dayandığı için `@UIState`'e nadiren ihtiyaç duyar; Task 1 dosyası olarak yine de eklenir ve Plan 05 ile paylaşılan kural olarak yazılır.
   **Plan 05 için kritik:** Plan 05'in görünümleri `@State` kullanıyorsa `swift build` Plan 06 başlamadan kırılır. O durumda Plan 06'yı başlatma, kullanıcıya bildir (bkz. "Varsayımlar" §A1).
2. **Odak çalmayan panel key olabiliyor.** `styleMask: [.nonactivatingPanel, .fullSizeContentView]` + `becomesKeyOnlyIfNeeded = true` + `override var canBecomeKey: Bool { true }` + `makeKeyAndOrderFront(nil)` sonucu: `panel visible=true key=true … appActive=false`. Yani panel key oluyor, uygulama öne gelmiyor. `NSApp.activate` çağrılmadı.
3. **`@FocusState` bir frame sonra gerçekten odaklıyor.** `.task { await Task.yield(); noteFocused = true }` sonrası `panel.firstResponder` → `SwiftUI.PlatformTextView`. Odak, uygulama aktif olmadan metin alanına gitti.
4. **SwiftUI `.keyboardShortcut` non-activating panelde çalışıyor ama modifier ayrımı güvenilir değil.** Paneldeki `Button(...).keyboardShortcut(.return, modifiers: .command)`, `⌘⇧↩` (shift'li) olayında da tetiklendi — iki buton (`⌘↩` ve `⌘⇧↩`) arasında ayrım yapılmadı. `NSEvent.addLocalMonitorForEvents(matching: .keyDown)` kurulup `nil` döndürüldüğünde ise olay SwiftUI'ya hiç ulaşmadı ve ayrım kesinleşti: `monitor cmd-return` ve `monitor cmd-shift-return` birer kez, doğru sırayla. **Karar: local key monitor otoriter**, `event.window === panel` koşuluyla sınırlı (aksi halde kütüphane penceresindeki `Esc` de yutulur).
5. **`cancelOperation(_:)` override'ı `Esc`'i yakalıyor** (metin alanı first responder olsa bile panel kapandı: `after esc panelVisible=false`). Monitor da aynı işi yaptığı için ikisi birlikte kullanılır; `orderOut` idempotenttir.
6. **`MenuBarExtra`'nın *label* görünümü açılışta `onAppear` alıyor.** Ölçüm: `label.onAppear connected=true` damgası `didFinishLaunching` damgasından **önce**. Yani `@Environment(\.openWindow)` / `\.openSettings` aksiyonlarını label görünümünden bir `@MainActor` köprüye yazmak, `applicationDidFinishLaunching` içinde kullanılabilir olmalarını garanti eder (menü hiç açılmasa bile).
7. **`openWindow(id: "library")` delegate kapsamından çalışıyor** ve SwiftUI `Window(_:id:)` sahnesinin `NSWindow.identifier?.rawValue` değeri **sahne id'sinin birebir aynısı**: ölçüm `library/Kütüphane/AppKitWindow`. Yani pencereyi AppKit tarafında `NSApp.windows.first { $0.identifier?.rawValue == "library" }` ile bulmak güvenli.
8. **`Window` sahnesi açılışta kendiliğinden açılmıyor** (`beforeOpen … windows=Item-0|NSStatusBarWindow`). `SHOTCUE_OPEN_LIBRARY=1` ile açmak için açıkça `openWindow` çağrılır.
9. **Aktivasyon politikası geçişleri çalışıyor**: `LibraryView` kökünde `.onAppear` → `setActivationPolicy(.regular)` (`policy=0`), `.onDisappear` → `.accessory` (`policy=1`). `NSApplication.ActivationPolicy` rawValue'ları: `.regular=0`, `.accessory=1`.
10. **`SMAppService.mainApp.status`** derlendi ve çalıştı; kurulu olmayan (geçici dizinden çalışan) bundle için `.notFound` döndü. Yani login item yalnızca `~/Applications/Shotcue.app` yolundan anlamlıdır ve durum **her açılışta okunur, cache'lenmez**.
11. **`UNUserNotificationCenterDelegate`'in async imzaları** (`userNotificationCenter(_:didReceive:) async` ve `userNotificationCenter(_:willPresent:) async -> UNNotificationPresentationOptions`) `NSObject, NSApplicationDelegate, UNUserNotificationCenterDelegate` conformance'ı ile, `.defaultIsolation(MainActor.self)` altında sınıfa ayrıca `@MainActor` yazmadan derlendi. `requestAuthorization(options:)` ve `notificationSettings()` async biçimleri de derlendi.
12. **`NSWorkspace.shared.openApplication(at:configuration:completionHandler:)`** + `NSWorkspace.OpenConfiguration().createsNewApplicationInstance = true` derlendi (relaunch yardımcısı).
13. **`NSScreen.screens.first { $0.frame.contains(NSEvent.mouseLocation) }`** derlendi ve çalıştı; panel `visibleFrame` sağ üstüne 16 pt boşlukla oturdu (`frame=(1274.0, 881.0, 420.0, 176.0)`, tek ekranlı makine). Panel yüksekliği `NSHostingView` içeriğe göre küçüldüğü için konum **içerik yerleştikten sonra** hesaplanır.
14. **CoreGraphics ikon üretimi + `CGImageDestination` PNG yazımı** `swift scripts/render-icon.swift` olarak interpreter modunda çalıştı (0,59 sn) ve 1024×1024 RGBA PNG üretti; `sips` + `iconutil -c icns` zinciri 364 KB `AppIcon.icns` verdi (`file` → `Mac OS X icon … "ic12" type`).
15. **`claude auth status` salt okunur ve JSON döndürüyor**: `{"loggedIn": true, "authMethod": "claude.ai", … "subscriptionType": "max"}`. Tanılama raporu bu JSON'un yalnızca `loggedIn`, `authMethod`, `subscriptionType` alanlarını yazar (e-posta/org id rapora girmez).
16. **`codesign -dv` çıktı biçimi**: ad-hoc imzada `flags=0x10002(adhoc,runtime)` ve `Authority=` satırı **yok**; self-signed sertifikada `Authority=Shotcue Dev` satırı çıkar. Manuel kontrol listesi bu satırı arar.
17. **`withObservationTracking`** ile `@Observable` bir ayar kutusunu izlemek derlendi; `onChange:` kapanışı `@Sendable` olduğu için içine non-Sendable closure **capture edilemez** — handler bir stored property'de tutulup `Task { @MainActor in … }` içinden çağrılır ve izleme her değişimden sonra yeniden kurulur (tek seferlik tetikleme).
18. **Bu planın 15 App dosyasının tamamı**, Plan 00–05'in imza sözleşmesine birebir uyan stub modüllerle (`ShotcueCore`, `ShotcuePersistence`, `ShotcueCapture`, `ShotcueNotes`, `ShotcueClaudeBridge`, `ShotcueUI` — hepsi Plan 00'ın hedef ayarlarıyla) kurulmuş bir pakette derlendi: `Build complete!`, **sıfır uyarı**. Yani aşağıdaki kod blokları birbirleriyle tutarlıdır; kırmızıya düşen tek şey sözleşmeden sapan bir Plan 01–05 imzası olabilir (Varsayımlar tablosu).
    - Stub derlemesi ayrıca bir **Plan 04 kısıtı** ortaya çıkardı: `TaskDispatcher.liveEvents(runID:)` `async` olmadığı için `RunCoordinator` bir `actor` olarak bu gereksinimi `nonisolated func liveEvents(...)` yazmadan karşılayamaz (`conformance ... crosses into actor-isolated code`). Plan 04 bunu yapmamışsa `swift build` Plan 06'dan önce kırılır; kullanıcıya bildir.

## Varsayımlar (Plan 05 ve 04 dosyaları henüz yokken sözleşmeden türetildi — uyuşmazsa yalnızca işaretli satırlar değişir)

Bu plan, kendisine verilen isim sözleşmesine harfiyen uyar. Sözleşmede **yer almayan** ve bu yüzden en makul biçimde varsayılan ayrıntılar aşağıdadır; her biri planda `// VARSAYIM Ax` yorumuyla işaretlidir, böylece uyuşmazlık tek satırlık düzeltmeyle kapanır.

| # | Varsayım | Nerede | Uyuşmazsa |
|---|---|---|---|
| A1 | Plan 05 görünümleri `@State` yerine `@UIState`/`@Observable` kullanır | tüm plan | `swift build` kırılır; Plan 06'yı başlatma, kullanıcıya bildir |
| A2 | `AppDatabase.open(at:)` dönüş tipi `AppDatabase`'dir | Task 1 | `private let database:` tipini düzelt |
| A3 | `PersistenceMaintenance.renumberSortIndexes(in:projectID:)`'in `in:` parametresi `AppDatabase`, `projectID:` `UUID?` | Task 1 | `renumber` closure gövdesi |
| A4 | `LibraryStore.renumber` tipi `((UUID?) async throws -> Void)?` | Task 1 | closure imzası |
| A5 | `LibraryStore.selectedTaskIDs` tipi `Set<UUID>` | Task 6 | atama satırı |
| A6 | `TranscriptionCoordinator(titleMaker:)` tipi `(String, String?, Date) -> String` | Task 1 | closure imzası |
| A7 | `SchedulerDriver(interval:)` tipi `Duration` (`.seconds(30)`) | Task 1 | `interval: 30` yaz |
| A8 | `SchedulerDriver` bir sınıf; `start()/stop()/tick()` `async` değil | Task 2 | `Task { await … }` ile sar |
| A9 | `DesktopHandoffService(composerRoute:)` bir enum; case'ler `.codeNew` / `.coworkNew` | Task 1 | spike S5 kararına göre case adı |
| A10 | `ProcessClaudeRunner(environmentOverrides:)` tipi `[String: String]` | Task 1 | `[:]` literalinin tipi |
| A11 | Plan 04 bildirim `userInfo`'suna `"taskID"` / `"runID"` anahtarlarını UUID string olarak yazar | Task 6 | `NotificationIDs` anahtar sabitleri |
| A12 | `QuickPanelStore` `capture(taskID:)`, `save()`, `saveAndSend()` ve `onClose` üyelerine sahip; `save`/`saveAndSend` `async` | Task 4 | `QuickPanelActions` closure gövdeleri (tek yer) |
| A13 | `SettingsView(projects:)` tipi `[Project]` | Task 2 | `AppStatusModel.projects` yerine ilgili store'u geç |
| A14 | `SettingsStore` `@Observable`'dır ve alanları doğrudan okunabilir | Task 1 | `SettingsObserver` yerine yalnızca pencere-kapanışı tetiklemesi kalır |
| A15 | `Plan 04` `ClaudeRunError` tipini sözleşmede vermediği için App kendi `AppServiceError.claudeNotFound` hatasını fırlatır | Task 1 | `ClaudeRunError.notFound` varsa onu fırlat |
| A16 | `WhisperKitTranscriber(modelName:)` `SettingsStore.sttModel` string'ini olduğu gibi kabul eder | Task 1 | eşleme fonksiyonu ekle |

## Dosya haritası (bu planın ürettikleri)

```
Sources/ShotcueApp/
  UIState.swift                    typealias UIState = SwiftUI.State (CLT'de @State makrosu yok)
  AppServiceError.swift            App katmanının kendi hataları + MissingClaudeRunner
  AppSettingsBridge.swift          SettingsStore → AppSettingsSnapshot → RunSettings / kök dizin eşlemesi (saf)
  AppStatusModel.swift             @Observable: claudeVersion, transcriberState, projects, loginItem, diagnostics
  AppEnvironment.swift             composition root: tüm somut servisler, store'lar, controller'lar, applySettings()
  SettingsObserver.swift           withObservationTracking ile SettingsStore izleme
  AppWindowCoordinator.swift       WindowOpener (openWindow köprüsü) + ActivationPolicyController
  ShotcueApp.swift                 @main App: MenuBarExtra + Window("Kütüphane") + Settings, sahne kökleri
  AppDelegate.swift                NSApplicationDelegate + UNUserNotificationCenterDelegate + NotificationIDs
  CaptureFlowController.swift      kısayol → izin → screencapture → task/capture kaydı → thumbnail → panel
  QuickPanelController.swift       QuickPanel (NSPanel), PanelPlacement, PanelKeyMonitor, controller
  OnboardingWindowController.swift NSWindow + OnboardingView + izin sonrası relaunch
  LoginItemManager.swift           SMAppService.mainApp sarmalayıcısı (durum asla cache'lenmez)
  Diagnostics.swift                claude --version / auth status / izin durumları → /tmp/shotcue-diagnostics.txt
  DiagnosticsBar.swift             Ayarlar altına eklenen "Tanılama çalıştır" şeridi
scripts/render-icon.swift          CoreGraphics ile 1024×1024 ikon PNG'si
scripts/make-icon.sh               render + sips + iconutil → Resources/AppIcon.icns
Resources/AppIcon.icns             üretilen ikon (bundle.sh zaten kopyalıyor)
docs/superpowers/plans/verification/   make shot çıktıları (Task 9)
```

Bağımlılık yönü: `ShotcueApp → {ShotcueUI, ShotcuePersistence, ShotcueCapture, ShotcueNotes, ShotcueClaudeBridge} → ShotcueCore`. `AppEnvironment` dışındaki hiçbir App dosyası servis modüllerinin **init'ini** çağırmaz; controller'lar yalnızca `AppServices` ve Core protokolleriyle çalışır.

---

### Task 1: AppEnvironment + AppSettingsBridge (composition root)

**Files:**
- Create: `Sources/ShotcueApp/UIState.swift`
- Create: `Sources/ShotcueApp/AppServiceError.swift`
- Create: `Sources/ShotcueApp/AppSettingsBridge.swift`
- Create: `Sources/ShotcueApp/AppStatusModel.swift`
- Create: `Sources/ShotcueApp/SettingsObserver.swift`
- Create: `Sources/ShotcueApp/AppEnvironment.swift`
- Modify: `Sources/ShotcueApp/ShotcueApp.swift` (Plan 00 Task 11'in `SpikeRunner`'ı bu task'ta silinir; `@main` gövdesi Task 2'de yeniden yazılır — bu task'ta yalnızca `SpikeRunner` bloğu ve `init()` çağrısı kaldırılır)
- Test: yok (App target'ının test hedefi yoktur; doğrulama `make build` + Step 8'deki `make run` gözlemi)

**Interfaces:**

Consumes (imzalar birebir; Plan 00–05):
```swift
// Plan 00 — ShotcueCore
FileStore(rootURL: URL)                                   // .rootURL, .databaseURL, .absoluteURL(for:), .relativePath(for:)
FileStore.defaultRoot(fileManager: FileManager = .default) -> URL
func ensureDirectories(fileManager: FileManager = .default) throws
func captureRelPath(id: UUID, date: Date, calendar: Calendar = .current) -> String
func thumbRelPath(id: UUID) -> String
SystemClock()                                             // Clock: var now: Date
KeyCombo { keyCode: UInt32, modifiers: UInt32, label: String }; KeyCombo.defaultCombo
ClaudePermissionMode: String { bypassPermissions, acceptEdits, dontAsk }
TitleMaker.title(noteText: String, transcript: String?, createdAt: Date, calendar: Calendar = .current) -> String
protocol ClaudeRunner { func run(_: RunSpec, onEvent: @escaping @Sendable (RunEvent) -> Void) async throws -> ClaudeRunResult
                        func cancel(runID: UUID) async; func version() async throws -> String }
protocol TranscriptionQueue { func enqueue(voiceNoteID: UUID) async; func processPending() async }
protocol Transcriber { var engineName: String { get }; func modelState() async -> TranscriberModelState
                       func downloadModel() async throws
                       func transcribe(fileURL: URL, language: String) async throws -> Transcript }
TranscriberModelState { notDownloaded, downloading(progress: Double), ready, failed(String) }

// Plan 01 — ShotcuePersistence
AppDatabase.open(at: URL) throws                          // → AppDatabase (VARSAYIM A2)
GRDBProjectRepository(database:) / GRDBTaskRepository(database:) / GRDBRunRepository(database:)
PersistenceMaintenance.renumberSortIndexes(in:projectID:) async throws

// Plan 02 — ShotcueCapture
ScreencaptureService(executableURL:environment:)          // varsayılanlar var → ScreencaptureService()
ImageIOThumbnailService(); SystemPermissionService(); CarbonHotKeyService()
PasteboardWriter.copyPNG(at:)                             // @MainActor

// Plan 03 — ShotcueNotes
EngineAudioRecorder(inputDeviceUID:)
WhisperKitTranscriber(modelName:modelsDirectory:)
TranscriptionCoordinator(transcriber:taskRepository:fileStore:language:titleMaker:)   // actor, setLanguage(_:)

// Plan 04 — ShotcueClaudeBridge
ClaudeLocator.locate(preferredPath:) -> URL?
ProcessClaudeRunner(executableURL:environmentOverrides:)
ShellGitInspector()
RunSettings(maxConcurrent:maxTurns:maxBudgetUSD:timeout:permissionMode:model:effort:extraSystemPrompt:keepAwake:)
RunCoordinator(runner:taskRepository:projectRepository:runRepository:gitInspector:fileStore:notifier:clock:settings:)
    // actor, TaskDispatcher; updateSettings(_:); recoverInterruptedRuns() async throws -> Int
SchedulerDriver(dispatcher:taskRepository:projectRepository:clock:calendar:interval:)  // start()/stop()/tick()
DesktopHandoffService(claudeExecutable:fileStore:composerRoute:)
UserNotificationNotifier(center:)

// Plan 05 — ShotcueUI
AppServices(projects:tasks:runs:capture:thumbnails:permissions:recorder:transcriber:
            transcriptionQueue:dispatcher:handoff:fileStore:clock:)
SettingsStore(defaults:)      // hotKeyLabel, launchAtLogin, copyToClipboardOnCapture, storageRootPath, claudePath,
                              // defaultModel, defaultEffort, maxTurns, maxBudgetUSD, timeoutMinutes, maxConcurrentRuns,
                              // permissionMode, extraSystemPrompt, keepAwake, sttLanguage, sttModel, inputDeviceUID,
                              // foundationModelsEnabled, lastUsedProjectID; computed hotKey: KeyCombo
LibraryStore(services:)       // + renumber closure, selectedTaskIDs
MenuBarStore(services:); PermissionsStore(services:); TaskDetailStore(services:taskID:)
QuickPanelStore(services:settings:)
```

Produces (Task 2–9 bunlara dayanır; `ShotcueApp` target'ı `.defaultIsolation(MainActor.self)` olduğu için
aşağıdaki sınıf/enum üyeleri ayrıca `@MainActor` yazılmadan MainActor-izole olur):
```swift
typealias UIState<Value> = SwiftUI.State<Value>
enum AppServiceError: LocalizedError, Equatable { case claudeNotFound, storageUnavailable(String) }
struct MissingClaudeRunner: ClaudeRunner
struct AppSettingsSnapshot: Equatable, Sendable        // 16 alan; tanımı Step 3'te
enum AppSettingsBridge {
    static func snapshot(of: SettingsStore) -> AppSettingsSnapshot
    static func runSettings(from: AppSettingsSnapshot) -> RunSettings
    static func storageRoot(from: SettingsStore) -> URL
    static func modelsDirectory(in: FileStore) -> URL
    static let defaultSTTModel: String
}
@Observable final class AppStatusModel {               // claudeVersion, claudeFound, transcriberState,
    func flashMenuBarIcon()                            // projects, loginItemStatusText, hotKeyError,
}                                                      // diagnosticsRunning, captureFlash, lastError
final class SettingsObserver { func start(_: SettingsStore, onChange: @escaping () -> Void); func stop() }
final class AppEnvironment {
    // let: settings, fileStore, services, notifier, hotKeys, runCoordinator, scheduler, transcription,
    //      diagnostics, status, settingsObserver, libraryStore, menuBarStore, permissionsStore,
    //      windowOpener, activationPolicy, quickPanel, onboarding, captureFlow
    init() throws
    func applySettings(); func registerHotKey(_: KeyCombo)
    func refreshStatus(); func refreshLoginItemStatus()
    func downloadTranscriberModel(); func runDiagnostics()
    func showOnboardingIfNeeded() async
    static func presentFatalAndExit(_: Error) -> Never
}
enum AppBootstrap { static let environment: AppEnvironment }
```

> Task 1, Task 2–7'nin ürettiği tipleri (`WindowOpener`, `ActivationPolicyController`, `QuickPanelController`, `OnboardingWindowController`, `CaptureFlowController`, `Diagnostics`) `AppEnvironment` içinde **kullanır**. Bu yüzden Task 1'in derlenmesi için bu tiplerin iskeletleri Step 6'da aynı task içinde, birer satırlık gövdeyle oluşturulur ve Task 2–7 onları doldurur. Böylece her task sonunda `make build` yeşil kalır.

- [ ] **Step 1: Ön koşulları doğrula (Plan 01–05 gerçekten bitmiş mi)**

Run:
```bash
swift build 2>&1 | tail -3
swift test 2>&1 | tail -3
grep -rn "@State " Sources/ShotcueUI | head -5
```
Expected: `Build complete!`, `Test run with … passed`, ve `grep` **hiçbir şey basmamalı**. `@State` bulunursa derleme zaten kırılmış olur (bkz. Doğrulanmış bulgu 1); bu durumda dur, Plan 05'e `typealias UIState` düzeltmesini uygula/uygulat, sonra devam et. `swift build` kırmızıysa Plan 06 başlamaz.

- [ ] **Step 2: `UIState.swift` ve `AppServiceError.swift`'i yaz**

`Sources/ShotcueApp/UIState.swift`:
```swift
import SwiftUI

/// Command Line Tools ships no `SwiftUIMacros` plugin, and the macOS 27 SDK declares SwiftUI's `State`
/// as an attached macro (`SwiftUICore`: `public macro State() = #externalMacro(module: "SwiftUIMacros", …)`)
/// in addition to the property-wrapper struct. In attribute position the macro wins, so plain `@State`
/// fails to build here with "plugin for module 'SwiftUIMacros' not found" — the same class of failure
/// that got `#Preview` banned in the Global Constraints.
///
/// Spelling the wrapper through a typealias resolves the attribute to `SwiftUI.State` instead.
/// Verified 2026-09-22 with CLT 27.0 / Swift 6.4: `@State var x: String = ""` fails, `@UIState` builds.
typealias UIState<Value> = SwiftUI.State<Value>
```

`Sources/ShotcueApp/AppServiceError.swift`:
```swift
import Foundation
import ShotcueCore

/// Errors raised by the composition root itself (spec §8).
enum AppServiceError: LocalizedError, Equatable {
    case claudeNotFound
    case storageUnavailable(String)

    var errorDescription: String? {
        switch self {
        case .claudeNotFound:
            return "`claude` komutu bulunamadı. Ayarlar > Claude'da yolu elle girin."
        case .storageUnavailable(let path):
            return "Depolama klasörü kullanılamıyor: \(path)"
        }
    }
}

/// Stand-in runner used when `claude` is missing, so the app still launches and Settings can show the
/// red status (spec §8: "claude bulunamadı → Ayarlar'da kırmızı durum; gönder düğmeleri devre dışı").
/// VARSAYIM A15: Plan 04 does not expose an error type in the contract, so App throws its own.
struct MissingClaudeRunner: ClaudeRunner {
    func run(_ spec: RunSpec, onEvent: @escaping @Sendable (RunEvent) -> Void) async throws -> ClaudeRunResult {
        throw AppServiceError.claudeNotFound
    }

    func cancel(runID: UUID) async {}

    func version() async throws -> String {
        throw AppServiceError.claudeNotFound
    }
}
```

- [ ] **Step 3: `AppSettingsBridge.swift`'i yaz (tek saf eşleme noktası)**

`Sources/ShotcueApp/AppSettingsBridge.swift`:
```swift
import Foundation
import ShotcueClaudeBridge
import ShotcueCore
import ShotcueUI

/// Value-type mirror of every `SettingsStore` field the app has to react to.
/// Equatable so `AppEnvironment.applySettings()` can skip no-op re-applications.
/// The last three fields (`sttModel`, `inputDeviceUID`, `storageRootPath`) cannot be applied to a live
/// service — they are baked into the transcriber, the recorder and the file store at init — but they are
/// part of the snapshot so a change is *noticed* and the user is told a restart is needed.
struct AppSettingsSnapshot: Equatable, Sendable {
    var maxConcurrent: Int
    var maxTurns: Int
    var maxBudgetUSD: Double
    var timeout: TimeInterval
    var permissionMode: ClaudePermissionMode
    var model: String?
    var effort: String?
    var extraSystemPrompt: String
    var keepAwake: Bool
    var sttLanguage: String
    var sttModel: String
    var inputDeviceUID: String?
    var hotKey: KeyCombo
    var launchAtLogin: Bool
    var copyToClipboardOnCapture: Bool
    var storageRootPath: String
}

/// The only place that translates user-facing settings into service inputs. Pure functions only:
/// this is the piece of App code worth reasoning about, so it must stay side-effect free.
enum AppSettingsBridge {
    static func snapshot(of settings: SettingsStore) -> AppSettingsSnapshot {
        AppSettingsSnapshot(
            maxConcurrent: max(1, settings.maxConcurrentRuns),
            maxTurns: max(1, settings.maxTurns),
            maxBudgetUSD: max(0.01, settings.maxBudgetUSD),
            timeout: TimeInterval(max(1, settings.timeoutMinutes) * 60),
            permissionMode: settings.permissionMode,
            model: nonEmpty(settings.defaultModel),
            effort: nonEmpty(settings.defaultEffort),
            extraSystemPrompt: settings.extraSystemPrompt,
            keepAwake: settings.keepAwake,
            sttLanguage: nonEmpty(settings.sttLanguage) ?? "tr",
            sttModel: nonEmpty(settings.sttModel) ?? defaultSTTModel,
            inputDeviceUID: nonEmpty(settings.inputDeviceUID),
            hotKey: settings.hotKey,
            launchAtLogin: settings.launchAtLogin,
            copyToClipboardOnCapture: settings.copyToClipboardOnCapture,
            storageRootPath: settings.storageRootPath)
    }

    static func runSettings(from snapshot: AppSettingsSnapshot) -> RunSettings {
        RunSettings(
            maxConcurrent: snapshot.maxConcurrent,
            maxTurns: snapshot.maxTurns,
            maxBudgetUSD: snapshot.maxBudgetUSD,
            timeout: snapshot.timeout,
            permissionMode: snapshot.permissionMode,
            model: snapshot.model,
            effort: snapshot.effort,
            extraSystemPrompt: snapshot.extraSystemPrompt,
            keepAwake: snapshot.keepAwake)
    }

    /// Empty setting → spec default `~/Library/Application Support/Shotcue`; otherwise the user's path
    /// with `~` expanded (the DB only ever stores paths relative to this root).
    static func storageRoot(from settings: SettingsStore) -> URL {
        guard let path = nonEmpty(settings.storageRootPath) else { return FileStore.defaultRoot() }
        return URL(fileURLWithPath: (path as NSString).expandingTildeInPath, isDirectory: true)
    }

    /// WhisperKit model cache lives inside the storage root so "Depolama konumu" moves it too.
    static func modelsDirectory(in fileStore: FileStore) -> URL {
        fileStore.rootURL.appendingPathComponent("models", isDirectory: true)
    }

    static let defaultSTTModel = "openai_whisper-large-v3-v20240930_turbo"

    private static func nonEmpty(_ value: String?) -> String? {
        guard let value, !value.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty else { return nil }
        return value
    }
}
```

- [ ] **Step 4: `AppStatusModel.swift` ve `SettingsObserver.swift`'i yaz**

`Sources/ShotcueApp/AppStatusModel.swift`:
```swift
import Foundation
import ShotcueCore

/// Everything the UI needs to know about the host machine rather than about the data model.
/// Held by `AppEnvironment` (a reference, not view state), so SwiftUI observes it without `@State`.
@Observable
final class AppStatusModel {
    var claudeVersion: String = "kontrol ediliyor…"
    var claudeFound: Bool = false
    var transcriberState: TranscriberModelState = .notDownloaded
    var projects: [Project] = []
    var loginItemStatusText: String = "bilinmiyor"
    var hotKeyError: String?
    var diagnosticsRunning: Bool = false
    var captureFlash: Bool = false
    var lastError: String?

    init() {}

    /// Highlights the menu bar icon right after a capture (spec §5.1 step 5).
    func flashMenuBarIcon() {
        captureFlash = true
        Task { @MainActor in
            try? await Task.sleep(for: .milliseconds(1200))
            captureFlash = false
        }
    }
}
```

`Sources/ShotcueApp/SettingsObserver.swift`:
```swift
import Foundation
import Observation
import ShotcueUI

/// Re-arming `withObservationTracking` watcher over `SettingsStore`.
///
/// `withObservationTracking`'s `onChange:` closure is `@Sendable`, so a non-Sendable handler cannot be
/// captured inside it (verified: "capture of 'onChange' with non-Sendable type '() -> Void' in a
/// '@Sendable' closure"). The handler is therefore stored and invoked after hopping back to the main
/// actor, and tracking is re-installed because each `withObservationTracking` fires exactly once.
final class SettingsObserver {
    private var handler: (() -> Void)?
    private var settings: SettingsStore?
    private var running = false

    init() {}

    func start(_ settings: SettingsStore, onChange: @escaping () -> Void) {
        self.settings = settings
        self.handler = onChange
        running = true
        track()
    }

    func stop() {
        running = false
        handler = nil
        settings = nil
    }

    private func track() {
        guard running, let settings else { return }
        withObservationTracking {
            // Reading the whole snapshot registers every field the app pushes into a service.
            _ = AppSettingsBridge.snapshot(of: settings)
        } onChange: { [weak self] in
            Task { @MainActor [weak self] in
                guard let self, self.running else { return }
                self.handler?()
                self.track()
            }
        }
    }
}
```

- [ ] **Step 5: `AppEnvironment.swift`'i yaz (composition root)**

`Sources/ShotcueApp/AppEnvironment.swift`:
```swift
import AppKit
import Foundation
import ShotcueCapture
import ShotcueClaudeBridge
import ShotcueCore
import ShotcueNotes
import ShotcuePersistence
import ShotcueUI

/// Single owner of every concrete service, store and AppKit controller (spec §6.7).
/// Nothing else in the app calls a service initialiser.
final class AppEnvironment {
    let settings: SettingsStore
    let fileStore: FileStore
    let services: AppServices
    let notifier: UserNotificationNotifier
    let hotKeys: CarbonHotKeyService
    let runCoordinator: RunCoordinator
    let scheduler: SchedulerDriver
    let transcription: TranscriptionCoordinator
    let diagnostics: Diagnostics
    let status: AppStatusModel
    let settingsObserver: SettingsObserver

    let libraryStore: LibraryStore
    let menuBarStore: MenuBarStore
    let permissionsStore: PermissionsStore

    let windowOpener: WindowOpener
    let activationPolicy: ActivationPolicyController
    let quickPanel: QuickPanelController
    let onboarding: OnboardingWindowController
    let captureFlow: CaptureFlowController

    private let database: AppDatabase                 // VARSAYIM A2
    private let claudeExecutable: URL?
    private var appliedSnapshot: AppSettingsSnapshot?

    init() throws {
        let settings = SettingsStore(defaults: .standard)
        self.settings = settings

        // 1. Storage root + directory layout (spec §6.3).
        let root = AppSettingsBridge.storageRoot(from: settings)
        let fileStore = FileStore(rootURL: root)
        do {
            try fileStore.ensureDirectories()
        } catch {
            throw AppServiceError.storageUnavailable(root.path)
        }
        self.fileStore = fileStore

        // 2. Database + repositories.
        let database = try AppDatabase.open(at: fileStore.databaseURL)
        self.database = database
        let projects = GRDBProjectRepository(database: database)
        let tasks = GRDBTaskRepository(database: database)
        let runs = GRDBRunRepository(database: database)

        // 3. Capture, thumbnails, permissions, hotkey.
        let capture = ScreencaptureService()
        let thumbnails = ImageIOThumbnailService()
        let permissions = SystemPermissionService()
        self.hotKeys = CarbonHotKeyService()

        // 4. Voice notes + transcription.
        let snapshot = AppSettingsBridge.snapshot(of: settings)
        let recorder = EngineAudioRecorder(inputDeviceUID: snapshot.inputDeviceUID)
        let transcriber = WhisperKitTranscriber(
            modelName: snapshot.sttModel,                                          // VARSAYIM A16
            modelsDirectory: AppSettingsBridge.modelsDirectory(in: fileStore))
        let transcription = TranscriptionCoordinator(
            transcriber: transcriber,
            taskRepository: tasks,
            fileStore: fileStore,
            language: snapshot.sttLanguage,
            titleMaker: { noteText, transcript, createdAt in                       // VARSAYIM A6
                TitleMaker.title(noteText: noteText, transcript: transcript, createdAt: createdAt)
            })
        self.transcription = transcription

        // 5. Claude runner. A missing binary must not stop the app from launching (spec §8).
        let clock = SystemClock()
        let notifier = UserNotificationNotifier(center: .current())
        self.notifier = notifier
        let claudeExecutable = ClaudeLocator.locate(preferredPath: settings.claudePath)
        self.claudeExecutable = claudeExecutable
        let runner: any ClaudeRunner =
            claudeExecutable.map {
                ProcessClaudeRunner(executableURL: $0, environmentOverrides: [:])   // VARSAYIM A10
            } ?? MissingClaudeRunner()
        let gitInspector = ShellGitInspector()
        let runCoordinator = RunCoordinator(
            runner: runner,
            taskRepository: tasks,
            projectRepository: projects,
            runRepository: runs,
            gitInspector: gitInspector,
            fileStore: fileStore,
            notifier: notifier,
            clock: clock,
            settings: AppSettingsBridge.runSettings(from: snapshot))
        self.runCoordinator = runCoordinator

        // 6. Scheduler + hand-off.
        self.scheduler = SchedulerDriver(
            dispatcher: runCoordinator,
            taskRepository: tasks,
            projectRepository: projects,
            clock: clock,
            calendar: .current,
            interval: .seconds(30))                                                // VARSAYIM A7
        let handoff = DesktopHandoffService(
            claudeExecutable: claudeExecutable,
            fileStore: fileStore,
            composerRoute: .codeNew)                                               // VARSAYIM A9

        // 7. The façade the UI module sees.
        let services = AppServices(
            projects: projects,
            tasks: tasks,
            runs: runs,
            capture: capture,
            thumbnails: thumbnails,
            permissions: permissions,
            recorder: recorder,
            transcriber: transcriber,
            transcriptionQueue: transcription,
            dispatcher: runCoordinator,
            handoff: handoff,
            fileStore: fileStore,
            clock: clock)
        self.services = services

        // 8. Stores (Plan 05).
        let libraryStore = LibraryStore(services: services)
        libraryStore.renumber = { projectID in                                     // VARSAYIM A3/A4
            try await PersistenceMaintenance.renumberSortIndexes(in: database, projectID: projectID)
        }
        self.libraryStore = libraryStore
        let permissionsStore = PermissionsStore(services: services)
        self.permissionsStore = permissionsStore
        self.menuBarStore = MenuBarStore(services: services)

        // 9. App-level state and AppKit controllers.
        let status = AppStatusModel()
        self.status = status
        self.settingsObserver = SettingsObserver()
        self.windowOpener = WindowOpener()
        let activationPolicy = ActivationPolicyController()
        self.activationPolicy = activationPolicy
        self.diagnostics = Diagnostics(
            claudeExecutable: claudeExecutable,
            permissions: permissions,
            fileStore: fileStore,
            settings: settings)
        let quickPanel = QuickPanelController(
            makeStore: { taskID in
                let store = QuickPanelStore(services: services, settings: settings)
                store.capture(taskID: taskID)                                      // VARSAYIM A12
                return store
            })
        self.quickPanel = quickPanel
        let onboarding = OnboardingWindowController(
            permissionsStore: permissionsStore,
            permissions: permissions,
            activationPolicy: activationPolicy)
        self.onboarding = onboarding
        self.captureFlow = CaptureFlowController(
            services: services,
            settings: settings,
            fileStore: fileStore,
            notifier: notifier,
            status: status,
            quickPanel: quickPanel,
            onboarding: onboarding)
    }

    // MARK: - Settings application

    /// Pushes changed settings into the services that cache them. Idempotent: identical snapshots are
    /// skipped, so it is safe to call from the observer, from the Settings window closing, and at launch.
    func applySettings() {
        let snapshot = AppSettingsBridge.snapshot(of: settings)
        guard snapshot != appliedSnapshot else { return }
        let previous = appliedSnapshot
        appliedSnapshot = snapshot

        let runSettings = AppSettingsBridge.runSettings(from: snapshot)
        let coordinator = runCoordinator
        Task { await coordinator.updateSettings(runSettings) }

        if previous?.sttLanguage != snapshot.sttLanguage {
            let transcription = transcription
            let language = snapshot.sttLanguage
            Task { await transcription.setLanguage(language) }
        }
        if previous?.hotKey != snapshot.hotKey {
            registerHotKey(snapshot.hotKey)
        }
        if previous?.launchAtLogin != snapshot.launchAtLogin {
            reconcileLoginItem(desired: snapshot.launchAtLogin)
        }
        // Settings that are baked in at init: tell the user instead of pretending they took effect.
        if let previous,
            previous.sttModel != snapshot.sttModel
                || previous.inputDeviceUID != snapshot.inputDeviceUID
                || previous.storageRootPath != snapshot.storageRootPath {
            status.lastError = "Bu ayar için Shotcue'yu yeniden başlatın (model / giriş cihazı / depolama)."
        }
    }

    /// (Re)registers the global hotkey. Carbon refuses a combination another app already owns; that is a
    /// user-visible condition, so it lands in `status.hotKeyError` rather than being swallowed.
    func registerHotKey(_ combo: KeyCombo) {
        hotKeys.unregister()
        let flow = captureFlow
        do {
            // The Carbon handler fires on an arbitrary thread; hop to the main actor before touching UI.
            try hotKeys.register(combo) {
                Task { @MainActor in flow.begin() }
            }
            status.hotKeyError = nil
        } catch {
            status.hotKeyError = "Kısayol \(combo.label) kaydedilemedi: \(error.localizedDescription)"
        }
    }

    private func reconcileLoginItem(desired: Bool) {
        do {
            try LoginItemManager.setEnabled(desired)
        } catch {
            status.lastError = "Girişte başlat ayarlanamadı: \(error.localizedDescription)"
        }
        refreshLoginItemStatus()
    }

    // MARK: - Status

    /// `claude --version`, transcriber model state, project list and login item status.
    func refreshStatus() {
        let transcriber = services.transcriber
        let projectRepository = services.projects
        Task { @MainActor in
            let version = await diagnostics.claudeVersion()
            status.claudeVersion = version.text
            status.claudeFound = version.found
            status.transcriberState = await transcriber.modelState()
            status.projects = (try? await projectRepository.allProjects()) ?? []
        }
        refreshLoginItemStatus()
    }

    /// Spec/research 01 §8: never cache the login item state, read it every time Settings opens.
    func refreshLoginItemStatus() {
        status.loginItemStatusText = LoginItemManager.statusText
    }

    func downloadTranscriberModel() {
        let transcriber = services.transcriber
        Task { @MainActor in
            status.transcriberState = .downloading(progress: 0)
            do {
                try await transcriber.downloadModel()
                status.transcriberState = await transcriber.modelState()
                await services.transcriptionQueue.processPending()
            } catch {
                status.transcriberState = .failed(error.localizedDescription)
            }
        }
    }

    func runDiagnostics() {
        guard !status.diagnosticsRunning else { return }
        status.diagnosticsRunning = true
        Task { @MainActor in
            defer { status.diagnosticsRunning = false }
            do {
                let url = try await diagnostics.writeReport(
                    transcriberState: status.transcriberState,
                    loginItemStatus: LoginItemManager.statusText,
                    hotKeyLabel: settings.hotKey.label)
                NSWorkspace.shared.open(url)
            } catch {
                status.lastError = "Tanılama yazılamadı: \(error.localizedDescription)"
            }
        }
    }

    /// First launch (or after a TCC reset): show onboarding when a required permission is missing.
    func showOnboardingIfNeeded() async {
        let screen = await services.permissions.state(of: .screenRecording)
        let microphone = await services.permissions.state(of: .microphone)
        guard screen != .granted || microphone != .granted else { return }
        onboarding.show()
    }

    // MARK: - Bootstrap

    static func presentFatalAndExit(_ error: Error) -> Never {
        let alert = NSAlert()
        alert.alertStyle = .critical
        alert.messageText = "Shotcue başlatılamadı"
        alert.informativeText = error.localizedDescription
        alert.addButton(withTitle: "Çık")
        alert.runModal()
        exit(1)
    }
}

/// Lazily built once, on the main actor, before the first scene body runs
/// (`AppDelegate.applicationWillFinishLaunching` touches it first to control the ordering).
enum AppBootstrap {
    static let environment: AppEnvironment = {
        do {
            return try AppEnvironment()
        } catch {
            AppEnvironment.presentFatalAndExit(error)
        }
    }()
}
```

- [ ] **Step 6: Task 2–7'nin tiplerini iskelet olarak oluştur (derleme yeşil kalsın)**

Aşağıdaki altı dosya **bu task'ta** minimal gövdeyle yazılır; Task 2–7 bunları doldurur. Gövdeler bilinçli olarak boş değil, "henüz bağlanmadı" durumunu log'layan gerçek kod:

`Sources/ShotcueApp/AppWindowCoordinator.swift`:
```swift
import AppKit

/// Bridges SwiftUI's `openWindow` / `openSettings` actions into AppKit scope (delegate, hotkey,
/// notification actions). Filled in Task 2.
final class WindowOpener {
    func connect(openWindow: @escaping (String) -> Void, openSettings: @escaping () -> Void) {
        _ = openWindow
        _ = openSettings
    }
    func openLibrary() {}
    func openSettingsWindow() {}
}

/// Owns `NSApp.setActivationPolicy` so the Dock icon appears only while a real window is on screen
/// (spec §6.6). Filled in Task 2.
final class ActivationPolicyController {
    func install() {}
    func begin(_ reason: String) {}
    func end(_ reason: String) {}
}
```

`Sources/ShotcueApp/QuickPanelController.swift`:
```swift
import AppKit
import ShotcueUI

/// Post-capture quick panel (spec §5.1, research 02 §6). Filled in Task 4.
final class QuickPanelController {
    private let makeStore: (UUID) -> QuickPanelStore
    init(makeStore: @escaping (UUID) -> QuickPanelStore) { self.makeStore = makeStore }
    func present(taskID: UUID) { _ = makeStore(taskID) }
    func dismiss() {}
}
```

`Sources/ShotcueApp/OnboardingWindowController.swift`:
```swift
import AppKit
import ShotcueCore
import ShotcueUI

/// Permission onboarding window (spec §6.1, research 02 §4). Filled in Task 5.
final class OnboardingWindowController {
    private let permissionsStore: PermissionsStore
    private let permissions: any PermissionService
    private let activationPolicy: ActivationPolicyController
    init(permissionsStore: PermissionsStore, permissions: any PermissionService,
         activationPolicy: ActivationPolicyController) {
        self.permissionsStore = permissionsStore
        self.permissions = permissions
        self.activationPolicy = activationPolicy
    }
    func show() {}
    func close() {}
}
```

`Sources/ShotcueApp/CaptureFlowController.swift` — Task 3'ün tam gövdesindeki `init` ve stored property'lerin
aynısı, `func begin() {}` boş: bu task'ta yalnızca imzalar gerekir, Task 3 Step 1 dosyayı bütünüyle yazar.
```swift
import AppKit
import ShotcueCore
import ShotcueUI

/// Hotkey → capture → task row → quick panel (spec §5.1). Filled in Task 3.
final class CaptureFlowController {
    private let services: AppServices
    private let settings: SettingsStore
    private let fileStore: FileStore
    private let notifier: any Notifier
    private let status: AppStatusModel
    private let quickPanel: QuickPanelController
    private let onboarding: OnboardingWindowController

    init(services: AppServices, settings: SettingsStore, fileStore: FileStore, notifier: any Notifier,
         status: AppStatusModel, quickPanel: QuickPanelController,
         onboarding: OnboardingWindowController) {
        self.services = services
        self.settings = settings
        self.fileStore = fileStore
        self.notifier = notifier
        self.status = status
        self.quickPanel = quickPanel
        self.onboarding = onboarding
    }

    func begin() {}
}
```

`Sources/ShotcueApp/LoginItemManager.swift`:
```swift
import Foundation
import ServiceManagement

/// `SMAppService.mainApp` wrapper. Filled in Task 7.
enum LoginItemManager {
    static var statusText: String { "bilinmiyor" }
    static func setEnabled(_ enabled: Bool) throws {}
    static func openSystemSettings() {}
}
```

`Sources/ShotcueApp/Diagnostics.swift`:
```swift
import Foundation
import ShotcueCore
import ShotcueUI

/// `claude --version` / `auth status` / permission report (spec §12, Plan 00 Task 11). Filled in Task 7.
struct Diagnostics {
    let claudeExecutable: URL?
    let permissions: any PermissionService
    let fileStore: FileStore
    let settings: SettingsStore

    static let reportURL = URL(fileURLWithPath: "/tmp/shotcue-diagnostics.txt")

    func claudeVersion() async -> (text: String, found: Bool) {
        (claudeExecutable == nil ? "bulunamadı" : "kontrol ediliyor…", claudeExecutable != nil)
    }

    func writeReport(
        transcriberState: TranscriberModelState, loginItemStatus: String, hotKeyLabel: String
    ) async throws -> URL {
        try "henüz bağlanmadı\n".write(to: Self.reportURL, atomically: true, encoding: .utf8)
        return Self.reportURL
    }
}
```

- [ ] **Step 7: Plan 00'ın `SpikeRunner`'ını kaldır**

`Sources/ShotcueApp/ShotcueApp.swift` içinden `enum SpikeRunner { … }` bloğunu ve `@main struct ShotcueApp` içindeki `init() { SpikeRunner.runIfRequested() }` satırlarını sil. Yerine hiçbir şey koyma; `@main` gövdesi Task 2'de tamamen yeniden yazılır. `SHOTCUE_SPIKE` ortam değişkeni bundan sonra desteklenmez; işlevi Task 7'deki `Diagnostics` devralır.

Run: `grep -rn "SpikeRunner\|SHOTCUE_SPIKE" Sources/ | wc -l`
Expected: `0`.

- [ ] **Step 8: Derle**

Run: `make build 2>&1 | tail -5`
Expected: `Build complete!`. Beklenen tek gürültü: `ld: warning: search path '/Library/Developer/CommandLineTools/Developer/…' not found` (iki satır, zararsız).

Hata çıkarsa tipik nedenler ve düzeltmeleri: `AppDatabase` tipi bulunamıyorsa Varsayım A2, `renumber` closure tipi uyuşmuyorsa A3/A4, `interval:` tipi uyuşmuyorsa A7 (`interval: 30`), `composerRoute:` case'i yoksa A9, `titleMaker:` closure'ı uyuşmuyorsa A6. Hiçbirini "geçici olarak yorum satırına alarak" geçme; sözleşmeyi Plan 05/04 dosyasından oku ve tek satırı düzelt.

- [ ] **Step 9: Uygulamayı kur, açılışın hâlâ ayakta olduğunu gör**

Run: `make run && sleep 3 && pgrep -x Shotcue && ls -la ~/Library/Application\ Support/Shotcue/`
Expected: `pgrep` bir PID basar; dizin listesinde `captures`, `thumbs`, `audio`, `runs` klasörleri ve (Plan 01 migration'ları koştuğu için) `shotcue.sqlite` görünür. Menü çubuğunda Plan 00'dan kalan kamera ikonu hâlâ duruyor (sahneler Task 2'de değişecek).

- [ ] **Step 10: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp
git commit -m "feat(app): add composition root and settings bridge

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: Sahneler, AppDelegate, pencere köprüsü ve aktivasyon politikası

**Files:**
- Modify: `Sources/ShotcueApp/ShotcueApp.swift` (tam yeniden yazım)
- Create: `Sources/ShotcueApp/AppDelegate.swift`
- Modify: `Sources/ShotcueApp/AppWindowCoordinator.swift` (Task 1 iskeletini doldur)
- Create: `Sources/ShotcueApp/DiagnosticsBar.swift`
- Test: yok (doğrulama: `make build` + Step 6/7 gözlemleri)

**Interfaces:**

Consumes:
```swift
AppBootstrap.environment                                 // Task 1
AppEnvironment.{settings, libraryStore, menuBarStore, permissionsStore, status, windowOpener,
                activationPolicy, quickPanel, onboarding, captureFlow, scheduler, runCoordinator,
                services, settingsObserver, applySettings(), refreshStatus(), refreshLoginItemStatus(),
                downloadTranscriberModel(), runDiagnostics(), showOnboardingIfNeeded()}
// Plan 05 views
MenuBarView(store:openLibrary:openSettings:quit:)
LibraryView(store:)
SettingsView(settings:permissions:projects:claudeVersion:transcriberState:onDownloadModel:)
// Plan 04
RunCoordinator.recoverInterruptedRuns() async throws -> Int
SchedulerDriver.start() / .stop() / .tick()
// Plan 00 protocols
TranscriptionQueue.processPending() async
```

Produces:
```swift
enum AppWindowID { static let library = "library" }
enum LaunchFlags { static var shouldOpenLibrary: Bool }
enum NotificationIDs { static let runDone, runFailed, open, terminal, retry, taskIDKey, runIDKey: String
                       static var categories: Set<UNNotificationCategory> }
final class WindowOpener   { func connect(openWindow:openSettings:); func openLibrary()
                             func openSettingsWindow(); static func libraryWindow() -> NSWindow? }
final class ActivationPolicyController { func install(); func begin(_: String); func end(_: String) }
final class AppDelegate: NSObject, NSApplicationDelegate, UNUserNotificationCenterDelegate
struct ShotcueApp: App          // @main
struct MenuBarLabel: View; struct MenuBarSceneRoot: View
struct LibrarySceneRoot: View; struct SettingsSceneRoot: View
struct DiagnosticsBar: View
```

- [ ] **Step 1: `AppWindowCoordinator.swift`'i doldur**

```swift
import AppKit

enum AppWindowID {
    /// SwiftUI sets `NSWindow.identifier` to the scene id, so AppKit can find this window by string
    /// (verified 2026-09-22: `NSApp.windows` → `library/Kütüphane/AppKitWindow`).
    static let library = "library"
}

enum LaunchFlags {
    /// `scripts/shot.sh` launches the app with this flag so screenshots have something to capture.
    static var shouldOpenLibrary: Bool {
        ProcessInfo.processInfo.environment["SHOTCUE_OPEN_LIBRARY"] == "1"
    }
}

/// Bridges SwiftUI's `openWindow` / `openSettings` actions into AppKit scope.
///
/// The actions only exist inside a `View`, but the delegate, the hotkey handler and notification
/// actions all need them. `MenuBarExtra`'s *label* view is instantiated while the app launches —
/// verified: its `onAppear` runs before `applicationDidFinishLaunching` — so connecting there makes
/// the actions available for the whole session even if the menu is never opened.
final class WindowOpener {
    private var openWindowAction: ((String) -> Void)?
    private var openSettingsAction: (() -> Void)?

    func connect(openWindow: @escaping (String) -> Void, openSettings: @escaping () -> Void) {
        self.openWindowAction = openWindow
        self.openSettingsAction = openSettings
    }

    func openLibrary() {
        guard let openWindowAction else {
            NSLog("[Shotcue] openWindow not connected yet; library request dropped")
            return
        }
        openWindowAction(AppWindowID.library)
        // The scene root flips the policy to .regular on appear; raise the window if it already existed.
        if let window = Self.libraryWindow() {
            window.makeKeyAndOrderFront(nil)
            NSApp.activate()
        }
    }

    func openSettingsWindow() {
        openSettingsAction?()
    }

    static func libraryWindow() -> NSWindow? {
        NSApp.windows.first { $0.identifier?.rawValue == AppWindowID.library }
    }
}

/// Dock visibility (spec §6.6, research 01 §8): menu-bar only by default, `.regular` while a real
/// window is on screen. Reference-counted because the library window and onboarding can overlap.
/// `.regular` requires an explicit `NSApp.activate()` afterwards, otherwise the window opens behind.
final class ActivationPolicyController {
    private var reasons: Set<String> = []

    static let libraryReason = "library"
    static let onboardingReason = "onboarding"

    /// Called once from `applicationDidFinishLaunching`: make the policy match reality in case a window
    /// already exists. Everything after that is driven by the scene root's `onAppear`/`onDisappear`,
    /// which was measured to fire reliably for `Window(_:id:)` (including on app termination), so no
    /// `NSWindow.willCloseNotification` backstop is needed — and an observer would have to smuggle the
    /// non-Sendable `Notification` into the main actor, which Swift 6 rejects.
    func install() {
        if WindowOpener.libraryWindow()?.isVisible == true {
            begin(Self.libraryReason)
        } else {
            apply()
        }
    }

    func begin(_ reason: String) {
        reasons.insert(reason)
        apply()
    }

    func end(_ reason: String) {
        reasons.remove(reason)
        apply()
    }

    private func apply() {
        if reasons.isEmpty {
            NSApp.setActivationPolicy(.accessory)
        } else {
            NSApp.setActivationPolicy(.regular)
            NSApp.activate()
        }
    }
}
```

- [ ] **Step 2: `DiagnosticsBar.swift`'i yaz**

`SettingsView`'in sözleşmedeki imzası bir tanılama parametresi taşımıyor; Ayarlar penceresinin kökü App'e ait olduğu için düğme `SettingsView`'in **altına** kendi şeridi olarak eklenir. (Plan 05 ileride `SettingsView(onRunDiagnostics:)` eklerse bu dosya silinip parametre kullanılır.)

```swift
import SwiftUI

/// "Tanılama çalıştır" strip below `SettingsView`; writes /tmp/shotcue-diagnostics.txt and opens it.
struct DiagnosticsBar: View {
    let status: AppStatusModel
    let claudeVersion: String
    let loginItemStatus: String
    let run: () -> Void

    var body: some View {
        HStack(spacing: 12) {
            VStack(alignment: .leading, spacing: 2) {
                Text("claude: \(claudeVersion)")
                    .font(.caption)
                    .foregroundStyle(status.claudeFound ? .secondary : Color.red)
                HStack(spacing: 6) {
                    Text("Girişte başlat: \(loginItemStatus)")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                    // macOS can park the registration in `requiresApproval`; this is the only way out.
                    Button("Giriş Öğeleri") { LoginItemManager.openSystemSettings() }
                        .buttonStyle(.link)
                        .font(.caption)
                }
                if let hotKeyError = status.hotKeyError {
                    Text(hotKeyError).font(.caption).foregroundStyle(Color.red)
                }
                if let lastError = status.lastError {
                    Text(lastError).font(.caption).foregroundStyle(Color.red)
                }
            }
            Spacer()
            Button("Tanılama çalıştır", action: run)
                .disabled(status.diagnosticsRunning)
            if status.diagnosticsRunning {
                ProgressView().controlSize(.small)
            }
        }
        .padding(.horizontal, 16)
        .padding(.vertical, 10)
    }
}
```

- [ ] **Step 3: `ShotcueApp.swift`'i yeniden yaz (sahneler)**

```swift
import AppKit
import SwiftUI
import ShotcueCore
import ShotcueUI

@main
struct ShotcueApp: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) private var delegate

    var body: some Scene {
        MenuBarExtra {
            MenuBarSceneRoot()
        } label: {
            MenuBarLabel()
        }
        .menuBarExtraStyle(.window)

        Window("Kütüphane", id: AppWindowID.library) {
            LibrarySceneRoot()
        }
        .defaultSize(width: 1180, height: 760)

        Settings {
            SettingsSceneRoot()
        }
    }
}

/// The menu bar icon. Also the app's one reliable "a view exists" moment during launch, so this is
/// where SwiftUI's window actions are handed to `WindowOpener` (verified: runs before
/// `applicationDidFinishLaunching`).
struct MenuBarLabel: View {
    @Environment(\.openWindow) private var openWindow
    @Environment(\.openSettings) private var openSettings

    private var environment: AppEnvironment { AppBootstrap.environment }

    var body: some View {
        Image(systemName: environment.status.captureFlash ? "checkmark.circle.fill" : "camera.viewfinder")
            .onAppear {
                environment.windowOpener.connect(
                    openWindow: { openWindow(id: $0) },
                    openSettings: { openSettings() })
            }
    }
}

struct MenuBarSceneRoot: View {
    private var environment: AppEnvironment { AppBootstrap.environment }

    var body: some View {
        MenuBarView(
            store: environment.menuBarStore,
            openLibrary: { environment.windowOpener.openLibrary() },
            openSettings: { environment.windowOpener.openSettingsWindow() },
            quit: { NSApplication.shared.terminate(nil) })
    }
}

struct LibrarySceneRoot: View {
    private var environment: AppEnvironment { AppBootstrap.environment }

    var body: some View {
        LibraryView(store: environment.libraryStore)
            .onAppear { environment.activationPolicy.begin(ActivationPolicyController.libraryReason) }
            .onDisappear { environment.activationPolicy.end(ActivationPolicyController.libraryReason) }
    }
}

struct SettingsSceneRoot: View {
    private var environment: AppEnvironment { AppBootstrap.environment }

    var body: some View {
        VStack(spacing: 0) {
            SettingsView(
                settings: environment.settings,
                permissions: environment.permissionsStore,
                projects: environment.status.projects,               // VARSAYIM A13
                claudeVersion: environment.status.claudeVersion,
                transcriberState: environment.status.transcriberState,
                onDownloadModel: { environment.downloadTranscriberModel() })
            Divider()
            DiagnosticsBar(
                status: environment.status,
                claudeVersion: environment.status.claudeVersion,
                loginItemStatus: environment.status.loginItemStatusText,
                run: { environment.runDiagnostics() })
        }
        .frame(minWidth: 660, minHeight: 560)
        .onAppear {
            // Login item status is read live every time Settings opens (research 01 §8).
            environment.refreshStatus()
        }
        .onDisappear {
            // Belt and braces next to SettingsObserver: apply whatever changed while the window was open.
            environment.applySettings()
        }
    }
}
```

- [ ] **Step 4: `AppDelegate.swift`'i yaz (yaşam döngüsü + bildirim kategorileri)**

Bu task'ta bildirim **aksiyon eşlemeleri** henüz boş bırakılır (Task 6 doldurur); kategori kaydı ve delegate ataması burada yapılır.

```swift
import AppKit
import ShotcueCore
import UserNotifications

/// Notification category / action identifiers. These strings are the contract with Plan 04's
/// `UserNotificationNotifier`; changing one here without changing it there silently drops actions.
enum NotificationIDs {
    static let runDone = "RUN_DONE"
    static let runFailed = "RUN_FAILED"
    static let open = "OPEN"
    static let terminal = "TERMINAL"
    static let retry = "RETRY"
    static let taskIDKey = "taskID"                                   // VARSAYIM A11
    static let runIDKey = "runID"                                     // VARSAYIM A11

    static var categories: Set<UNNotificationCategory> {
        let openAction = UNNotificationAction(identifier: open, title: "Aç", options: [.foreground])
        let terminalAction = UNNotificationAction(
            identifier: terminal, title: "Terminalde devam et", options: [])
        let retryAction = UNNotificationAction(
            identifier: retry, title: "Yeniden çalıştır", options: [])
        return [
            UNNotificationCategory(
                identifier: runDone, actions: [openAction, terminalAction],
                intentIdentifiers: [], options: []),
            UNNotificationCategory(
                identifier: runFailed, actions: [openAction, retryAction],
                intentIdentifiers: [], options: []),
        ]
    }
}

final class AppDelegate: NSObject, NSApplicationDelegate, UNUserNotificationCenterDelegate {
    /// Builds the environment before any scene body runs, so a failure can still show an alert.
    func applicationWillFinishLaunching(_ notification: Notification) {
        _ = AppBootstrap.environment
    }

    func applicationDidFinishLaunching(_ notification: Notification) {
        let environment = AppBootstrap.environment

        // Menu-bar-only by default (Info.plist LSUIElement is already true; this makes it explicit
        // and covers the case where a window opened and closed before we got here).
        NSApp.setActivationPolicy(.accessory)
        environment.activationPolicy.install()

        let center = UNUserNotificationCenter.current()
        center.delegate = self
        center.setNotificationCategories(NotificationIDs.categories)

        // Settings → services. First call pushes everything (run settings, STT language, hotkey, login item).
        environment.settingsObserver.start(environment.settings) {
            AppBootstrap.environment.applySettings()
        }
        environment.applySettings()
        environment.refreshStatus()

        Task { @MainActor in
            await Self.requestNotificationAuthorizationIfNeeded()

            // Spec §8: a run that was interrupted by quitting must not stay "running" forever.
            do {
                let recovered = try await environment.runCoordinator.recoverInterruptedRuns()
                if recovered > 0 {
                    NSLog("[Shotcue] marked %d interrupted run(s) as failed", recovered)
                }
            } catch {
                environment.status.lastError =
                    "Yarım kalan çalışmalar işaretlenemedi: \(error.localizedDescription)"
            }

            // Voice notes recorded while the model was missing get transcribed now.
            await environment.services.transcriptionQueue.processPending()

            // Scheduler owns its own 30 s DispatchSourceTimer and the NSWorkspace.didWakeNotification
            // observer (spec §6.5); the app only starts it and asks for one immediate catch-up pass.
            environment.scheduler.start()                              // VARSAYIM A8
            environment.scheduler.tick()

            await environment.showOnboardingIfNeeded()
        }

        if LaunchFlags.shouldOpenLibrary {
            environment.windowOpener.openLibrary()
        }
    }

    func applicationWillTerminate(_ notification: Notification) {
        let environment = AppBootstrap.environment
        environment.scheduler.stop()
        environment.hotKeys.unregister()
        environment.settingsObserver.stop()
    }

    /// Menu-bar app: closing the library window must not quit.
    func applicationShouldTerminateAfterLastWindowClosed(_ sender: NSApplication) -> Bool { false }

    func applicationSupportsSecureRestorableState(_ app: NSApplication) -> Bool { true }

    /// Clicking the Dock icon (only visible while .regular) reopens the library.
    func applicationShouldHandleReopen(_ sender: NSApplication, hasVisibleWindows flag: Bool) -> Bool {
        if !flag { AppBootstrap.environment.windowOpener.openLibrary() }
        return true
    }

    static func requestNotificationAuthorizationIfNeeded() async {
        let center = UNUserNotificationCenter.current()
        let settings = await center.notificationSettings()
        guard settings.authorizationStatus == .notDetermined else { return }
        _ = try? await center.requestAuthorization(options: [.alert, .sound])
    }

    // MARK: - UNUserNotificationCenterDelegate (actions wired in Task 6)

    func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        didReceive response: UNNotificationResponse
    ) async {
        NSLog("[Shotcue] notification action %@ (not wired yet)", response.actionIdentifier)
    }

    /// Banners while Shotcue is frontmost, too — a finished run is the whole point of the app.
    func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        willPresent notification: UNNotification
    ) async -> UNNotificationPresentationOptions {
        [.banner, .sound, .list]
    }
}
```

- [ ] **Step 5: Derle**

Run: `make build 2>&1 | tail -5`
Expected: `Build complete!`.

`MenuBarView`/`LibraryView`/`SettingsView` parametre adları uyuşmuyorsa sözleşmeyi Plan 05'in dosyasından oku ve **yalnızca** çağrı satırlarını düzelt; Plan 05 dosyalarına dokunma.

- [ ] **Step 6: Menü çubuğu, kütüphane penceresi ve Dock davranışını gör**

Run:
```bash
make run && sleep 3
pgrep -x Shotcue && osascript -e 'tell application "System Events" to count (every process whose name is "Shotcue")'
open -a ~/Applications/Shotcue.app --env SHOTCUE_OPEN_LIBRARY=1 && sleep 3
./scripts/shot.sh Shotcue /tmp/shotcue-shots && ls -la /tmp/shotcue-shots
```
Expected:
- Menü çubuğunda kamera ikonu var, Dock'ta **ikon yok** (uygulama `.accessory`).
- `SHOTCUE_OPEN_LIBRARY=1` ile açıldığında "Kütüphane" penceresi görünür ve **Dock'ta ikon belirir** (`.regular`).
- `shot.sh` en az bir `window-N.png` basar (Ekran Kaydı izni Terminal/Claude'a verilmiş olmalı).
- Kütüphane penceresini kapat → Dock ikonu kaybolur, uygulama menü çubuğunda kalır (`pgrep -x Shotcue` hâlâ PID basar).

Gözlemleri task notuna yaz: Dock ikonu geldi/gitti mi, pencere başlığı "Kütüphane" mi.

- [ ] **Step 7: Menü ve Ayarlar penceresini gör**

Run: menü çubuğu ikonuna tıkla → "Kütüphane" ve "Ayarlar" düğmelerine bas.
Expected: Menüden "Kütüphane" pencereyi açar (Dock ikonu gelir); "Ayarlar" Ayarlar penceresini açar ve **altında** "Tanılama çalıştır" şeridi görünür; `claude: …` satırı Task 7'ye kadar "kontrol ediliyor…" veya "bulunamadı" yazar. Ayarlar penceresini kapat → konsola hata düşmez (`log stream --predicate 'process == "Shotcue"'` ile bakılabilir).

- [ ] **Step 8: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp
git commit -m "feat(app): add scenes, delegate and window coordination

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: CaptureFlowController (kısayol → yakalama → kayıt → panel)

**Files:**
- Modify: `Sources/ShotcueApp/CaptureFlowController.swift` (Task 1 iskeletini doldur)
- Test: yok (doğrulama: `make build` + Step 3–6 gözlemleri)

**Interfaces:**

Consumes:
```swift
// Plan 00 — ShotcueCore
protocol CaptureService { func captureRegion(to destination: URL) async throws -> CaptureResult? }  // nil = iptal
CaptureResult { fileURL: URL, width: Int, height: Int, scale: Double }
protocol ThumbnailService { func makeThumbnail(from: URL, to: URL, maxPixel: Int) async throws }
protocol PermissionService { func state(of: PermissionKind) async -> PermissionState
                             func request(_: PermissionKind) async -> PermissionState
                             func openSystemSettings(for: PermissionKind) }
PermissionKind { screenRecording, microphone, notifications }; PermissionState { notDetermined, granted, denied }
protocol TaskRepository { func tasks(projectID: UUID?) async throws -> [ShotTask]
                          func save(_ task: ShotTask) async throws; func save(_ capture: Capture) async throws }
protocol Notifier { func notify(_ notification: AppNotification) async }
AppNotification(kind:title:body:taskID:runID:)                       // kind: .runDone | .runFailed
ShotTask(id:projectID:title:noteText:status:mode:modelOverride:sortIndex:scheduledAt:titleEditedByUser:createdAt:updatedAt:)
Capture(id:taskID:relPath:thumbRelPath:width:height:scale:createdAt:)
SortIndex.between(_:_:) -> Double
TitleMaker.title(noteText:transcript:createdAt:calendar:) -> String
FileStore.captureRelPath(id:date:calendar:) / .thumbRelPath(id:) / .absoluteURL(for:) / .ensureParentDirectory(for:)
Clock.now

// Plan 02 — ShotcueCapture
PasteboardWriter.copyPNG(at:)                                        // @MainActor

// Task 1/2/4/5 (bu plan)
AppServices.{capture, thumbnails, permissions, tasks, clock}
SettingsStore.copyToClipboardOnCapture
AppStatusModel.flashMenuBarIcon() / .lastError
QuickPanelController.present(taskID:)
OnboardingWindowController.show()
```

Produces:
```swift
final class CaptureFlowController {
    init(services:settings:fileStore:notifier:status:quickPanel:onboarding:)
    func begin()          // hotkey entry point; re-entrancy guarded
}
```

- [ ] **Step 1: `CaptureFlowController.swift`'i doldur**

```swift
import AppKit
import ShotcueCapture
import ShotcueCore
import ShotcueUI

/// Spec §5.1: global hotkey → region selection → PNG moved into the store → `ShotTask` + `Capture`
/// rows → background thumbnail → optional clipboard copy → quick panel.
final class CaptureFlowController {
    private let services: AppServices
    private let settings: SettingsStore
    private let fileStore: FileStore
    private let notifier: any Notifier
    private let status: AppStatusModel
    private let quickPanel: QuickPanelController
    private let onboarding: OnboardingWindowController
    private var isCapturing = false

    init(services: AppServices, settings: SettingsStore, fileStore: FileStore, notifier: any Notifier,
         status: AppStatusModel, quickPanel: QuickPanelController,
         onboarding: OnboardingWindowController) {
        self.services = services
        self.settings = settings
        self.fileStore = fileStore
        self.notifier = notifier
        self.status = status
        self.quickPanel = quickPanel
        self.onboarding = onboarding
    }

    /// Entry point for the global hotkey (called on the main actor by `AppEnvironment.registerHotKey`).
    /// A second press while `screencapture -i -s` owns the mouse would put two selection cursors on
    /// screen, so re-entrant presses are dropped instead of queued.
    func begin() {
        guard !isCapturing else { return }
        isCapturing = true
        Task { @MainActor in
            defer { isCapturing = false }
            await run()
        }
    }

    private func run() async {
        // 1. Permission gate. Spec §8: until Screen Recording is granted the hotkey opens onboarding.
        let screenRecording = await services.permissions.state(of: .screenRecording)
        guard screenRecording == .granted else {
            onboarding.show()
            return
        }

        // 2. Region selection into a temporary file; nil means the user pressed Esc (no task is created).
        let temporaryURL = FileManager.default.temporaryDirectory
            .appendingPathComponent("shotcue-capture-\(UUID().uuidString).png")
        let captured: CaptureResult?
        do {
            captured = try await services.capture.captureRegion(to: temporaryURL)
        } catch {
            try? FileManager.default.removeItem(at: temporaryURL)
            await report(title: "Yakalama başarısız", error: error)
            return
        }
        guard let captured else {
            try? FileManager.default.removeItem(at: temporaryURL)
            return
        }

        // 3. Move the PNG to captures/YYYY/MM/<uuid>.png (spec §6.3; the DB stores the relative path).
        let captureID = UUID()
        let now = services.clock.now
        let relPath = fileStore.captureRelPath(id: captureID, date: now)
        let destination = fileStore.absoluteURL(for: relPath)
        do {
            try fileStore.ensureParentDirectory(for: relPath)
            if FileManager.default.fileExists(atPath: destination.path) {
                try FileManager.default.removeItem(at: destination)
            }
            try FileManager.default.moveItem(at: captured.fileURL, to: destination)
        } catch {
            try? FileManager.default.removeItem(at: temporaryURL)
            await report(title: "Yakalama diske yazılamadı", error: error)
            return
        }

        // 4. Rows. The task starts in `inbox` without a project; the quick panel assigns the project and
        //    performs the inbox → ready transition (spec §7), so the state machine stays in one place.
        let inboxTasks = (try? await services.tasks.tasks(projectID: nil)) ?? []
        let task = ShotTask(
            title: TitleMaker.title(noteText: "", transcript: nil, createdAt: now),
            status: .inbox,
            mode: .implement,
            sortIndex: SortIndex.between(inboxTasks.map(\.sortIndex).max(), nil),
            createdAt: now,
            updatedAt: now)
        let capture = Capture(
            id: captureID,
            taskID: task.id,
            relPath: relPath,
            width: captured.width,
            height: captured.height,
            scale: captured.scale,
            createdAt: now)
        do {
            try await services.tasks.save(task)
            try await services.tasks.save(capture)
        } catch {
            try? FileManager.default.removeItem(at: destination)
            await report(title: "Yakalama kaydedilemedi", error: error)
            return
        }

        // 5. Thumbnail off the main actor; a missing thumbnail is cosmetic and regenerated later.
        makeThumbnail(for: capture)

        // 6. Clipboard (spec §5.1 step 3, optional setting).
        if settings.copyToClipboardOnCapture {
            PasteboardWriter.copyPNG(at: destination)
        }

        // 7. Quick panel + a short menu bar highlight (spec §5.1 step 5).
        quickPanel.present(taskID: task.id)
        status.flashMenuBarIcon()
    }

    private func makeThumbnail(for capture: Capture) {
        let thumbnails = services.thumbnails
        let tasks = services.tasks
        let store = fileStore
        let source = store.absoluteURL(for: capture.relPath)
        let thumbRelPath = store.thumbRelPath(id: capture.id)
        let destination = store.absoluteURL(for: thumbRelPath)
        Task.detached(priority: .utility) {
            do {
                try store.ensureParentDirectory(for: thumbRelPath)
                try await thumbnails.makeThumbnail(from: source, to: destination, maxPixel: 512)
                var updated = capture
                updated.thumbRelPath = thumbRelPath
                try await tasks.save(updated)
            } catch {
                NSLog("[Shotcue] thumbnail failed for %@: %@", capture.id.uuidString, "\(error)")
            }
        }
    }

    /// Spec §8: capture failures produce a notification plus a visible error, and no task row.
    private func report(title: String, error: Error) async {
        status.lastError = "\(title): \(error.localizedDescription)"
        await notifier.notify(
            AppNotification(kind: .runFailed, title: title, body: error.localizedDescription))
    }
}
```

- [ ] **Step 2: Derle**

Run: `make build 2>&1 | tail -4`
Expected: `Build complete!`.

`Capture` alanlarının `var` olduğuna güveniyoruz (Plan 00 Task 1 `public var thumbRelPath: String?`). Derleme `updated.thumbRelPath` satırında patlarsa Plan 00'ın modelini oku, `Capture`'ı yeniden oluşturarak kaydet (`Capture(id: capture.id, taskID: …, thumbRelPath: thumbRelPath, …)`).

- [ ] **Step 3: Kısayolun yakalamayı açtığını gör**

Run: `make run && sleep 3` → ardından `⌃⇧2`'ye bas.
Expected: Ekranda macOS'un bölge seçme imleci (artı imleci) çıkar. Bir bölge seç.
- Hızlı panel ekranın **sağ üstünde** açılır, not alanı odaklıdır (imleç yanıp söner, hemen yazmaya başlayabilirsin), **öndeki uygulama değişmez** (arkadaki uygulamanın başlık çubuğu hâlâ aktif renkte).
- Menü çubuğu ikonu ~1,2 sn onay işaretine dönüp kamera ikonuna geri döner.

Run (doğrulama):
```bash
ls -R ~/Library/Application\ Support/Shotcue/captures | tail -5
ls ~/Library/Application\ Support/Shotcue/thumbs | tail -3
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select status, title from task order by created_at desc limit 1; select width, height, scale, thumb_rel_path from capture order by created_at desc limit 1;"
```
Expected: `captures/2026/09/<uuid>.png` var; `thumbs/<uuid>.jpg` birkaç saniye içinde oluşur; task satırı `inbox` durumunda ve başlığı `Yakalama <gün.ay saat:dk>`; capture satırında gerçek piksel boyutu, `scale` 2 (Retina) ve dolu `thumb_rel_path`.

- [ ] **Step 4: İptalin hiçbir şey yaratmadığını gör**

Run: `⌃⇧2` → seçim imleci çıkınca **Esc**.
Expected: Panel açılmaz, bildirim çıkmaz, yeni PNG/satır oluşmaz:
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite "select count(*) from task;"
```
iki kez çalıştırıldığında aynı sayıyı verir.

- [ ] **Step 5: Panoya kopyalama ayarını gör**

Ayarlar > Genel'de "Yakalamada panoya kopyala"yı aç, Ayarlar'ı kapat, `⌃⇧2` ile bir bölge yakala.
Run: `osascript -e 'clipboard info' | head -3`
Expected: Çıktıda `«class PNGf»` görünür (PNG pano üzerinde). Ayar kapalıyken aynı komut önceki pano içeriğini gösterir.

- [ ] **Step 6: Re-entrancy korumasını gör**

Run: `⌃⇧2`'ye bas, seçim imleci ekranda dururken `⌃⇧2`'ye 3 kez daha bas, sonra bir bölge seç.
Expected: Tek seçim imleci, tek panel, tek task satırı (`select count(*) from task` bir artar).

- [ ] **Step 7: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp/CaptureFlowController.swift
git commit -m "feat(app): wire hotkey capture flow

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: QuickPanelController (odak çalmayan NSPanel)

**Files:**
- Modify: `Sources/ShotcueApp/QuickPanelController.swift` (Task 1 iskeletini doldur; `QuickPanel`, `PanelPlacement`, `PanelKeyMonitor` ve controller aynı dosyada — hepsi tek sorumluluğun parçası: panelin yaşam döngüsü)
- Test: yok (doğrulama: `make build` + Step 3–7 gözlemleri)

**Interfaces:**

Consumes:
```swift
QuickPanelStore(services:settings:)          // Plan 05; .capture(taskID:), .onClose, .save(), .saveAndSend()
QuickPanelView(store:)                       // Plan 05
```

Produces:
```swift
final class QuickPanel: NSPanel {            // canBecomeKey == true, cancelOperation → onCancel
    init(contentView: NSView); var onCancel: (() -> Void)?
}
enum PanelPlacement {
    static let inset: CGFloat
    static func screenUnderMouse() -> NSScreen?
    static func topRightOrigin(for size: NSSize) -> NSPoint
}
final class PanelKeyMonitor {
    func install(for: NSWindow, onSave: (() -> Void)?, onSaveAndSend: (() -> Void)?, onCancel: @escaping () -> Void)
    func remove()
}
final class QuickPanelController {
    init(makeStore: @escaping (UUID) -> QuickPanelStore)
    func present(taskID: UUID); func dismiss()
}
```

- [ ] **Step 1: `QuickPanelController.swift`'i doldur**

```swift
import AppKit
import SwiftUI
import ShotcueUI

/// Post-capture quick panel (spec §5.1 / §6.6, research 02 §6 recipe).
///
/// The style mask deliberately has no `.titled`: the panel is chrome-less and draggable by its
/// background. Every line below is load-bearing; the comments name what breaks without it.
final class QuickPanel: NSPanel {
    init(contentView view: NSView) {
        super.init(
            contentRect: NSRect(x: 0, y: 0, width: 420, height: 460),
            styleMask: [.nonactivatingPanel, .fullSizeContentView],
            backing: .buffered,
            defer: false)
        isFloatingPanel = true
        level = .floating                       // stays above the app the user is working in
        hidesOnDeactivate = false               // spec §6.6: clicking outside must NOT close it
        becomesKeyOnlyIfNeeded = true           // key only when the text field actually needs it
        collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        // without .canJoinAllSpaces the panel vanishes on a Space switch; without .fullScreenAuxiliary
        // it never shows over a full-screen app (research 02 §6 pitfall 7)
        animationBehavior = .utilityWindow
        isMovableByWindowBackground = true
        titleVisibility = .hidden
        titlebarAppearsTransparent = true
        isReleasedWhenClosed = false            // the controller decides the lifetime
        hasShadow = true
        standardWindowButton(.closeButton)?.isHidden = true
        standardWindowButton(.miniaturizeButton)?.isHidden = true
        standardWindowButton(.zoomButton)?.isHidden = true
        // If QuickPanelView paints its own rounded glass surface, also set:
        //   backgroundColor = .clear; isOpaque = false
        // Left on the system window background by default so the panel can never render invisible.
        self.contentView = view
    }

    /// Required. With `.nonactivatingPanel` and no `.titled`, `canBecomeKey` defaults to false and the
    /// panel's TextField/TextEditor never receives a keystroke (research 02 §6 pitfall 1).
    override var canBecomeKey: Bool { true }

    /// The panel must never become the main window; that is what would make the app look "activated".
    override var canBecomeMain: Bool { false }

    var onCancel: (() -> Void)?

    /// Esc travels up the responder chain to here even while the SwiftUI text view is first responder
    /// (verified 2026-09-22). The key monitor does the same; `orderOut` is idempotent.
    override func cancelOperation(_ sender: Any?) {
        onCancel?()
    }
}

enum PanelPlacement {
    static let inset: CGFloat = 16

    /// The display the mouse is on — i.e. the display the user just captured from (spec §5.1).
    /// `NSEvent.mouseLocation` is in global screen coordinates, same space as `NSScreen.frame`.
    static func screenUnderMouse() -> NSScreen? {
        let point = NSEvent.mouseLocation
        return NSScreen.screens.first { $0.frame.contains(point) } ?? NSScreen.main
    }

    /// Top-right corner of the usable area (below the menu bar, clear of the Dock), 16 pt inset.
    static func topRightOrigin(for size: NSSize) -> NSPoint {
        guard let screen = screenUnderMouse() else { return NSPoint(x: inset, y: inset) }
        let frame = screen.visibleFrame
        return NSPoint(x: frame.maxX - size.width - inset, y: frame.maxY - size.height - inset)
    }
}

/// Esc / ⌘↩ / ⌘⇧↩ handling for the quick panel.
///
/// Why a monitor instead of relying on SwiftUI `.keyboardShortcut`: measured 2026-09-22 inside exactly
/// this panel, a `Button(...).keyboardShortcut(.return, modifiers: .command)` also fired for ⌘⇧↩, so
/// "Kaydet" and "Kaydet ve gönder" could not be distinguished. A local key-down monitor returning nil
/// consumes the event before SwiftUI sees it and the modifier test becomes exact (verified: one
/// `cmd-return` and one `cmd-shift-return`, no double fire). Scoped to `event.window === panel` so the
/// library window's own Esc/⌘↩ handling is untouched.
final class PanelKeyMonitor {
    private var monitor: Any?

    private static let escapeKeyCode: UInt16 = 53      // kVK_Escape
    private static let returnKeyCode: UInt16 = 36      // kVK_Return

    func install(for panel: NSWindow, onSave: (() -> Void)?, onSaveAndSend: (() -> Void)?,
                 onCancel: @escaping () -> Void) {
        remove()
        monitor = NSEvent.addLocalMonitorForEvents(matching: .keyDown) { [weak panel] event in
            guard let panel, event.window === panel else { return event }
            let flags = event.modifierFlags.intersection(.deviceIndependentFlagsMask)
            switch event.keyCode {
            case Self.escapeKeyCode:
                onCancel()
                return nil
            case Self.returnKeyCode where flags.contains(.command):
                if flags.contains(.shift) {
                    guard let onSaveAndSend else { return event }
                    onSaveAndSend()
                } else {
                    guard let onSave else { return event }
                    onSave()
                }
                return nil
            default:
                return event
            }
        }
    }

    func remove() {
        if let monitor { NSEvent.removeMonitor(monitor) }
        monitor = nil
    }
}

final class QuickPanelController {
    private let makeStore: (UUID) -> QuickPanelStore
    private var panel: QuickPanel?
    private var store: QuickPanelStore?
    private let keyMonitor = PanelKeyMonitor()

    init(makeStore: @escaping (UUID) -> QuickPanelStore) {
        self.makeStore = makeStore
    }

    /// Shows the panel for a freshly captured task. A second capture replaces the contents rather than
    /// stacking windows, so there is never more than one quick panel on screen.
    func present(taskID: UUID) {
        dismiss()

        let store = makeStore(taskID)
        store.onClose = { [weak self] in self?.dismiss() }
        self.store = store

        let hosting: NSHostingView<QuickPanelView> = NSHostingView(rootView: QuickPanelView(store: store))
        hosting.sizingOptions = [.intrinsicContentSize]
        hosting.layoutSubtreeIfNeeded()
        let fitting = hosting.fittingSize

        let panel = QuickPanel(contentView: hosting)
        panel.onCancel = { [weak self] in self?.dismiss() }
        self.panel = panel

        // Size to the hosted content first, then place: NSHostingView shrinks the panel to its
        // intrinsic height, so the origin must be computed from the final frame (verified).
        panel.setContentSize(
            NSSize(width: max(420, fitting.width), height: max(180, fitting.height)))
        panel.setFrameOrigin(PanelPlacement.topRightOrigin(for: panel.frame.size))

        // NEVER NSApp.activate here (Global Constraints / research 02 §6 pitfall 2): the panel takes
        // keyboard focus without pulling the user out of the app they were working in.
        panel.makeKeyAndOrderFront(nil)

        keyMonitor.install(
            for: panel,
            onSave: { [weak self] in self?.commit(sendAfterwards: false) },        // VARSAYIM A12
            onSaveAndSend: { [weak self] in self?.commit(sendAfterwards: true) },  // VARSAYIM A12
            onCancel: { [weak self] in self?.dismiss() })
    }

    /// Esc / close: the capture stays in the inbox without a project (spec §5.1 step 4).
    func dismiss() {
        keyMonitor.remove()
        panel?.onCancel = nil
        panel?.orderOut(nil)
        panel = nil
        store?.onClose = nil
        store = nil
    }

    private func commit(sendAfterwards: Bool) {
        guard let store else { return }
        Task { @MainActor in
            if sendAfterwards {
                await store.saveAndSend()
            } else {
                await store.save()
            }
            dismiss()
        }
    }
}
```

> Plan 05'in `QuickPanelStore`'unda `save()` / `saveAndSend()` yoksa (Varsayım A12): `onSave`/`onSaveAndSend` parametrelerine `nil` geç. Monitor o iki kombinasyonu SwiftUI'ya bırakır; `QuickPanelView` içindeki `.keyboardShortcut`'lar çalışır ama ⌘⇧↩ ile ⌘↩ ayrımı garanti değildir (ölçülen davranış) — bunu task notuna yaz ve kullanıcıya bildir.

- [ ] **Step 2: Derle**

Run: `make build 2>&1 | tail -4`
Expected: `Build complete!`.

- [ ] **Step 3: Panelin odağı ve öne çıkmama davranışını gör**

Run: TextEdit'i aç ve içine yazı yaz (TextEdit ön uygulama olsun). Sonra `⌃⇧2` ile bir bölge yakala.
Expected:
- Panel ekranın sağ üstünde açılır; **TextEdit'in başlık çubuğu aktif renkte kalır** ve Dock'ta Shotcue ikonu belirmez (uygulama aktive edilmedi).
- Klavyeden hemen yaz: metin **panelin not alanına** gider, TextEdit'e gitmez.

Run (kanıt): `log stream --predicate 'process == "Shotcue"' --style compact` ile paralel izleme opsiyonel; asıl kanıt ekran gözlemi + `./scripts/shot.sh Shotcue /tmp/shotcue-shots` çıktısındaki panel PNG'sidir.

- [ ] **Step 4: ⌘↩ ve ⌘⇧↩ ayrımını gör**

Panel açıkken not alanına "test notu" yaz, proje seçiciden bir proje seç, `⌘↩`'ye bas.
Expected: Panel kapanır; task `ready` olur ve **run başlamaz**:
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select status, title, project_id is not null from task order by updated_at desc limit 1; select count(*) from run;"
```
→ `ready|test notu|1` ve run sayısı değişmemiş.

Yeni bir yakalama yap, proje seç, `⌘⇧↩`'ye bas.
Expected: Panel kapanır; task `queued` → `running` olur ve **bir run satırı** oluşur (`select count(*) from run;` bir artar). ⌘⇧↩'nin yanlışlıkla "Kaydet" gibi davranmadığı böyle kanıtlanır.

- [ ] **Step 5: Esc davranışını gör**

Yeni bir yakalama yap, hiçbir şey yazmadan `Esc`'e bas.
Expected: Panel kapanır; task `inbox` ve `project_id` NULL kalır (spec §5.1: "görüntü inbox'ta projesiz kalır"). Kütüphanede Inbox bölümünde görünür.

- [ ] **Step 6: Dışarı tıklamada kapanmadığını ve Space davranışını gör**

Panel açıkken başka bir uygulamaya tıkla.
Expected: Panel **kapanmaz** ve üstte kalır (`hidesOnDeactivate = false`). Mission Control ile başka bir Space'e geç → panel yeni Space'te de görünür (`.canJoinAllSpaces`). Bir uygulamayı tam ekran yap → panel hâlâ üstte (`.fullScreenAuxiliary`).

- [ ] **Step 7: İkinci ekranda konumlandırmayı gör (harici ekran varsa)**

Fareyi ikinci ekrana taşı, `⌃⇧2` ile o ekrandan bir bölge yakala.
Expected: Panel **ikinci ekranın** sağ üstünde açılır (menü çubuğunun ve Dock'un altında kalmadan, 16 pt boşlukla). Tek ekranlı makinede bu adım "ekran yok — atlandı" olarak not edilir ve Task 9'un kontrol listesinde açık kalır.

- [ ] **Step 8: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp/QuickPanelController.swift
git commit -m "feat(app): add non-activating quick panel

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: OnboardingWindowController (izinler + relaunch)

**Files:**
- Modify: `Sources/ShotcueApp/OnboardingWindowController.swift` (Task 1 iskeletini doldur)
- Test: yok (doğrulama: `make build` + Step 3–5 gözlemleri, `make reset-tcc` ile tekrarlanabilir)

**Interfaces:**

Consumes:
```swift
OnboardingView(permissions:onDone:)                                  // Plan 05; permissions: PermissionsStore
PermissionsStore(services:)                                          // Plan 05
protocol PermissionService { func state(of:) async -> PermissionState
                             func request(_:) async -> PermissionState
                             func openSystemSettings(for:) }          // Plan 02 SystemPermissionService
ActivationPolicyController.begin(_:) / .end(_:)                       // Task 2
```

Produces:
```swift
final class OnboardingWindowController {
    init(permissionsStore:permissions:activationPolicy:)
    func show(); func close()
    static func relaunchAfterPermissionGrant()
}
```

- [ ] **Step 1: `OnboardingWindowController.swift`'i doldur**

```swift
import AppKit
import SwiftUI
import ShotcueCore
import ShotcueUI

/// First-launch permission onboarding (spec §6.1, research 02 §4).
///
/// A plain `NSWindow` rather than a SwiftUI `Window` scene: onboarding is driven from AppKit scope
/// (launch, hotkey-without-permission) and must be able to appear before any scene has ever been
/// opened, then disappear without leaving a scene identifier behind.
final class OnboardingWindowController {
    private let permissionsStore: PermissionsStore
    private let permissions: any PermissionService
    private let activationPolicy: ActivationPolicyController
    private var window: NSWindow?
    /// Screen Recording state when the window opened, so we only relaunch on a real transition.
    private var screenRecordingWasGranted = false

    init(permissionsStore: PermissionsStore, permissions: any PermissionService,
         activationPolicy: ActivationPolicyController) {
        self.permissionsStore = permissionsStore
        self.permissions = permissions
        self.activationPolicy = activationPolicy
    }

    func show() {
        if let window {
            window.makeKeyAndOrderFront(nil)
            NSApp.activate()
            return
        }

        let service = permissions
        Task { @MainActor in
            screenRecordingWasGranted = await service.state(of: .screenRecording) == .granted
        }

        let hosting: NSHostingView<OnboardingView> = NSHostingView(
            rootView: OnboardingView(
                permissions: permissionsStore,
                onDone: { [weak self] in self?.finish() }))
        hosting.sizingOptions = [.intrinsicContentSize]

        let window = NSWindow(
            contentRect: NSRect(x: 0, y: 0, width: 560, height: 480),
            styleMask: [.titled, .closable, .fullSizeContentView],
            backing: .buffered,
            defer: false)
        window.title = "Shotcue Kurulumu"
        window.contentView = hosting
        window.isReleasedWhenClosed = false
        window.collectionBehavior = [.canJoinAllSpaces]
        window.center()
        self.window = window

        // Onboarding is a real, focusable window, so the app becomes a regular app while it is up.
        activationPolicy.begin(ActivationPolicyController.onboardingReason)
        window.makeKeyAndOrderFront(nil)
    }

    func close() {
        window?.orderOut(nil)
        window = nil
        activationPolicy.end(ActivationPolicyController.onboardingReason)
    }

    /// "Bitti": if Screen Recording flipped to granted while the window was open, the app has to be
    /// restarted before `screencapture` will actually work (research 02 §4.1: "İzin verildikten sonra
    /// uygulamayı yeniden başlatmak gerekir").
    private func finish() {
        let service = permissions
        let wasGranted = screenRecordingWasGranted
        Task { @MainActor in
            let isGranted = await service.state(of: .screenRecording) == .granted
            close()
            if isGranted && !wasGranted {
                Self.relaunchAfterPermissionGrant()
            }
        }
    }

    /// Relaunches the installed bundle and exits. Only meaningful for a real `.app`; when running the
    /// bare SwiftPM binary there is nothing to relaunch, so it just logs.
    static func relaunchAfterPermissionGrant() {
        let bundleURL = Bundle.main.bundleURL
        guard bundleURL.pathExtension == "app" else {
            NSLog("[Shotcue] not running from an .app bundle; skipping relaunch")
            return
        }
        let configuration = NSWorkspace.OpenConfiguration()
        configuration.createsNewApplicationInstance = true
        configuration.activates = true
        NSWorkspace.shared.openApplication(at: bundleURL, configuration: configuration) { _, error in
            if let error {
                NSLog("[Shotcue] relaunch failed: %@", "\(error)")
                return
            }
            DispatchQueue.main.async { exit(0) }
        }
    }
}
```

- [ ] **Step 2: Derle**

Run: `make build 2>&1 | tail -4`
Expected: `Build complete!`.

`OnboardingView(permissions:onDone:)` parametre adları uyuşmuyorsa yalnızca bu çağrıyı düzelt.

- [ ] **Step 3: Sıfır izinle ilk açılışı gör**

Run:
```bash
make reset-tcc
make run && sleep 3
```
Expected: "Shotcue Kurulumu" penceresi açılır (Dock'ta ikon belirir), içinde Ekran Kaydı ve Mikrofon için durum satırları ve "Sistem Ayarları'nda aç" düğmeleri var. `pgrep -x Shotcue` PID basar.

- [ ] **Step 4: Sistem Ayarları linklerini ve izin verme + relaunch akışını gör**

Ekran Kaydı satırındaki "Sistem Ayarları'nda aç"a bas.
Expected: System Settings > Privacy & Security > **Screen & System Audio Recording** paneli açılır (macOS 26'da panelin adı bu). Shotcue'yu aç, sistem "uygulamayı yeniden başlat" diyalogu gösterirse **Later**'ı seç (relaunch'ı Shotcue yapacak).

Onboarding penceresine dön, "Bitti"ye bas.
Expected: Uygulama **kendini yeniden başlatır**: `pgrep -x Shotcue` yeni bir PID gösterir (öncekinden farklı), menü çubuğu ikonu tekrar belirir, onboarding penceresi bir daha açılmaz (izin artık `granted`).

Run (kanıt):
```bash
pgrep -x Shotcue     # relaunch öncesi PID'i not al, sonra tekrar çalıştır
```

- [ ] **Step 5: İzinsizken kısayolun onboarding'i açtığını gör**

Run: `make reset-tcc && make run && sleep 3` → onboarding penceresini kapat (kırmızı düğme) → `⌃⇧2`'ye bas.
Expected: Seçim imleci **çıkmaz**; onboarding penceresi yeniden açılır (spec §8: "kısayol izin verilene kadar onboarding'i açar"). Task satırı oluşmaz.

- [ ] **Step 6: Mikrofonun akışı bloklamadığını gör**

Ekran Kaydı verilmiş, mikrofon reddedilmiş durumda `⌃⇧2` ile yakala.
Expected: Onboarding açılmaz (ekran kaydı yeterli), panel açılır, mikrofon düğmesi devre dışı/açıklamalı (Plan 05'in `QuickPanelView` davranışı), metin notu yazılabilir (spec §8).

- [ ] **Step 7: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp/OnboardingWindowController.swift
git commit -m "feat(app): add permission onboarding window and relaunch

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: Bildirim aksiyonları (Aç / Terminalde devam et / Yeniden çalıştır)

**Files:**
- Modify: `Sources/ShotcueApp/AppDelegate.swift` (Task 2'de log'layan `didReceive` gövdesini doldur)
- Test: yok (doğrulama: `make build` + Step 3–5 gözlemleri, bildirimler `RunCoordinator`'ın gerçek run'ıyla tetiklenir)

**Interfaces:**

Consumes:
```swift
// Plan 04
UserNotificationNotifier(center:)            // kategori id'leri RUN_DONE / RUN_FAILED,
                                             // aksiyon id'leri OPEN / TERMINAL / RETRY
protocol HandoffService { func openInTerminal(sessionID: String) throws
                          func openInDesktop(sessionID: String) throws
                          func openDesktopComposer(prompt: String, projectPath: String, files: [String]) throws }
protocol TaskDispatcher { func enqueue(taskID: UUID) async throws; func cancel(taskID: UUID) async
                          func runQueueNow() async; func setPaused(_: Bool) async
                          func isPaused() async -> Bool; func liveEvents(runID: UUID) -> AsyncStream<RunEvent> }
protocol RunRepository { func runs(taskID: UUID) async throws -> [Run]; func run(id: UUID) async throws -> Run? }
Run { id: UUID (= claude session id), taskID, state, startedAt, … }

// Task 1/2
AppEnvironment.{windowOpener, libraryStore, services, status}
NotificationIDs.{open, terminal, retry, taskIDKey, runIDKey}
LibraryStore.selectedTaskIDs                 // Set<UUID>  (VARSAYIM A5)
```

Produces: `AppDelegate.userNotificationCenter(_:didReceive:)` davranışı — aşağıdaki eşleme. Yeni public tip üretmez.

| actionIdentifier | Davranış |
|---|---|
| `OPEN` ve `UNNotificationDefaultActionIdentifier` (bildirime tıklama) | Kütüphaneyi aç (`openWindow(id: "library")` + `.regular`), `userInfo`'daki task'ı seç |
| `TERMINAL` | `handoff.openInTerminal(sessionID:)`; `runID` yoksa task'ın **en yeni** run'ının id'si kullanılır |
| `RETRY` | `dispatcher.enqueue(taskID:)` (task `queued`'a geçer, kuyruk pompalanır) |
| `UNNotificationDismissActionIdentifier` / bilinmeyen | Hiçbir şey |

- [ ] **Step 1: `didReceive` gövdesini yaz**

`Sources/ShotcueApp/AppDelegate.swift` içindeki `userNotificationCenter(_:didReceive:)`'i şununla değiştir:

```swift
    func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        didReceive response: UNNotificationResponse
    ) async {
        let environment = AppBootstrap.environment
        let userInfo = response.notification.request.content.userInfo
        let taskID = Self.uuid(userInfo[NotificationIDs.taskIDKey])
        let runID = Self.uuid(userInfo[NotificationIDs.runIDKey])

        switch response.actionIdentifier {
        case NotificationIDs.open, UNNotificationDefaultActionIdentifier:
            environment.windowOpener.openLibrary()
            if let taskID {
                environment.libraryStore.selectedTaskIDs = [taskID]        // VARSAYIM A5
            }

        case NotificationIDs.terminal:
            // `Run.id` doubles as the Claude session id (Plan 00 Task 1), so the session to resume is
            // either the run the notification came from or the task's newest run.
            guard let sessionID = await Self.sessionID(runID: runID, taskID: taskID, environment: environment)
            else {
                environment.status.lastError = "Devam edilecek oturum bulunamadı."
                return
            }
            do {
                try environment.services.handoff.openInTerminal(sessionID: sessionID)
            } catch {
                environment.status.lastError = "Terminal açılamadı: \(error.localizedDescription)"
            }

        case NotificationIDs.retry:
            guard let taskID else { return }
            do {
                try await environment.services.dispatcher.enqueue(taskID: taskID)
            } catch {
                environment.status.lastError = "Yeniden çalıştırılamadı: \(error.localizedDescription)"
            }

        default:
            break
        }
    }

    private static func uuid(_ value: Any?) -> UUID? {
        guard let string = value as? String else { return nil }
        return UUID(uuidString: string)
    }

    private static func sessionID(
        runID: UUID?, taskID: UUID?, environment: AppEnvironment
    ) async -> String? {
        if let runID { return runID.uuidString }
        guard let taskID else { return nil }
        let runs = (try? await environment.services.runs.runs(taskID: taskID)) ?? []
        return runs.max(by: { $0.startedAt < $1.startedAt })?.id.uuidString
    }
```

- [ ] **Step 2: Derle**

Run: `make build 2>&1 | tail -4`
Expected: `Build complete!`.

`LibraryStore.selectedTaskIDs` `Set<UUID>` değilse (Varsayım A5) atamayı o tipe uyarla (`[taskID]` bir `Array` ise aynı literal çalışır).

- [ ] **Step 3: Başarılı run bildirimini ve "Aç" aksiyonunu gör**

Kurulum: Kütüphaneden bir proje ekle (gerçek bir git repo yolu), bir yakalama al, not yaz, `⌘⇧↩` ile gönder.
Expected: Run bitince bildirim çıkar: başlık task adı, gövdede tek satır özet + maliyet, aksiyonlar **Aç** ve **Terminalde devam et**.

Bildirimin gövdesine tıkla (varsayılan aksiyon).
Expected: Kütüphane penceresi açılır/öne gelir, Dock ikonu belirir ve **o task seçili** gelir (Inspector sağda o task'ı gösterir).

- [ ] **Step 4: "Terminalde devam et" aksiyonunu gör**

Aynı bildirimi tekrar üret (bir task'ı yeniden gönder), bildirimin üzerine gelip "Terminalde devam et"e bas.
Expected: Varsayılan terminal uygulaması açılır ve içinde `claude --resume <session-id>` komutu çalışır; oturum geçmişi görünür. Session id, Kütüphane > task > run geçmişinde yazan id ile aynıdır:
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select id, state from run order by started_at desc limit 1;"
```

- [ ] **Step 5: "Yeniden çalıştır" aksiyonunu gör**

Başarısız bir run üret: Ayarlar > Claude'da "Max tur" değerini `1` yap, Ayarlar'ı kapat, bir task gönder.
Expected: Run `failed` (`subtype = error_max_turns`) olur, bildirim `RUN_FAILED` kategorisiyle çıkar, aksiyonlar **Aç** ve **Yeniden çalıştır**.

"Yeniden çalıştır"a bas.
Expected: Task `queued` → `running` olur, **yeni** bir run satırı oluşur:
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select count(*) from run where task_id = (select id from task order by updated_at desc limit 1);"
```
→ sayı bir artmış. Sonra "Max tur"u eski değerine (50) geri al.

- [ ] **Step 6: Uygulama öndeyken banner çıktığını gör**

Kütüphane penceresi ön plandayken bir task gönder.
Expected: Run bitince banner **yine görünür** (`willPresent` → `[.banner, .sound, .list]`), Bildirim Merkezi'nde de listelenir.

- [ ] **Step 7: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp/AppDelegate.swift
git commit -m "feat(app): handle run notification actions

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: LoginItemManager ve Diagnostics

**Files:**
- Modify: `Sources/ShotcueApp/LoginItemManager.swift` (Task 1 iskeletini doldur)
- Modify: `Sources/ShotcueApp/Diagnostics.swift` (Task 1 iskeletini doldur)
- Test: yok (doğrulama: `make build` + Step 3–6 gözlemleri)

**Interfaces:**

Consumes:
```swift
SMAppService.mainApp                         // ServiceManagement; .status, .register(), .unregister()
SMAppService.openSystemSettingsLoginItems()
protocol PermissionService { func state(of: PermissionKind) async -> PermissionState }
PermissionKind { screenRecording, microphone, notifications }
FileStore.{rootURL, databaseURL}
SettingsStore.{claudePath, sttModel, hotKey}
TranscriberModelState { notDownloaded, downloading(progress:), ready, failed(String) }
```

Produces:
```swift
enum LoginItemManager {
    static var statusText: String
    static func setEnabled(_ enabled: Bool) throws
    static func openSystemSettings()
}
struct ProcessOutput: Sendable { status: Int32, stdout: String, stderr: String, timedOut: Bool }
struct Diagnostics {
    static let reportURL: URL                // /tmp/shotcue-diagnostics.txt
    func claudeVersion() async -> (text: String, found: Bool)
    func writeReport(transcriberState:loginItemStatus:hotKeyLabel:) async throws -> URL
    static func run(_ executable: URL, _ arguments: [String], timeout: TimeInterval = 20) async -> ProcessOutput
}
```

- [ ] **Step 1: `LoginItemManager.swift`'i doldur**

```swift
import Foundation
import ServiceManagement

/// Launch at login (research 01 §8). The *only* source of truth is `SMAppService.mainApp.status`:
/// the toggle in Settings and the real registration can drift (the user can flip it in System
/// Settings), so the status is read on every Settings open and never cached.
///
/// Measured 2026-09-22: a bundle running from outside a normal apps directory reports `.notFound`,
/// so launch-at-login only works after `make install` puts the app in `~/Applications`.
enum LoginItemManager {
    static var statusText: String {
        switch SMAppService.mainApp.status {
        case .notRegistered:
            return "kapalı"
        case .enabled:
            return "etkin"
        case .requiresApproval:
            return "Sistem Ayarları > Genel > Giriş Öğeleri'nde onay bekliyor"
        case .notFound:
            return "kullanılamıyor (uygulama ~/Applications'a kurulmalı)"
        @unknown default:
            return "bilinmiyor"
        }
    }

    /// Idempotent: registering an already-enabled service throws, so the current status is checked first.
    static func setEnabled(_ enabled: Bool) throws {
        let service = SMAppService.mainApp
        if enabled {
            guard service.status != .enabled else { return }
            try service.register()
        } else {
            guard service.status == .enabled || service.status == .requiresApproval else { return }
            try service.unregister()
        }
    }

    static func openSystemSettings() {
        SMAppService.openSystemSettingsLoginItems()
    }
}
```

- [ ] **Step 2: `Diagnostics.swift`'i doldur**

Bu, Plan 00 Task 11'in `SpikeRunner`'ının kalıcı hâlidir: aynı ölçümleri (bundle içinden `claude` auth'u, izin durumları, imza) artık Ayarlar'dan tek düğmeyle alır.

```swift
import AppKit
import Foundation
import ShotcueCore
import ShotcueUI

struct ProcessOutput: Sendable {
    var status: Int32
    var stdout: String
    var stderr: String
    var timedOut: Bool
}

/// Read-only host diagnostics (spec §12; replaces Plan 00 Task 11's temporary `SpikeRunner`).
/// Nothing here mutates state: `claude --version` and `claude auth status` only read, and the report
/// deliberately keeps just `loggedIn` / `authMethod` / `subscriptionType` out of the auth JSON so the
/// account e-mail and org id never land in a file under /tmp.
struct Diagnostics {
    let claudeExecutable: URL?
    let permissions: any PermissionService
    let fileStore: FileStore
    let settings: SettingsStore

    static let reportURL = URL(fileURLWithPath: "/tmp/shotcue-diagnostics.txt")

    /// Shown in Settings > Claude (spec §6.4: the tested version is 2.1.278).
    func claudeVersion() async -> (text: String, found: Bool) {
        guard let claudeExecutable else { return ("bulunamadı", false) }
        let result = await Self.run(claudeExecutable, ["--version"])
        guard result.status == 0, !result.timedOut else {
            return ("okunamadı (exit \(result.status))", false)
        }
        let text = result.stdout.trimmingCharacters(in: .whitespacesAndNewlines)
        return (text.isEmpty ? "okunamadı" : text, true)
    }

    func writeReport(
        transcriberState: TranscriberModelState, loginItemStatus: String, hotKeyLabel: String
    ) async throws -> URL {
        let text = await report(
            transcriberState: transcriberState,
            loginItemStatus: loginItemStatus,
            hotKeyLabel: hotKeyLabel)
        try text.write(to: Self.reportURL, atomically: true, encoding: .utf8)
        return Self.reportURL
    }

    private func report(
        transcriberState: TranscriberModelState, loginItemStatus: String, hotKeyLabel: String
    ) async -> String {
        let version = await claudeVersion()
        let auth = await authSummary()
        let signature = await signatureSummary()
        let screenRecording = await permissions.state(of: .screenRecording)
        let microphone = await permissions.state(of: .microphone)
        let notifications = await permissions.state(of: .notifications)

        let formatter = ISO8601DateFormatter()
        formatter.timeZone = .current
        var lines: [String] = []
        lines.append("Shotcue tanılama — \(formatter.string(from: Date()))")
        lines.append("app: \(Bundle.main.bundleURL.path)")
        lines.append(
            "sürüm: \(Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "?")"
                + " (\(Bundle.main.infoDictionary?["CFBundleVersion"] as? String ?? "?"))")
        lines.append("imza: \(signature)")
        lines.append("claude yolu: \(claudeExecutable?.path ?? "bulunamadı")")
        lines.append("claude ayarı: \(settings.claudePath.isEmpty ? "(otomatik)" : settings.claudePath)")
        lines.append("claude sürümü: \(version.text)")
        lines.append("claude auth: \(auth)")
        lines.append(
            "izinler: ekran kaydı=\(screenRecording.rawValue)"
                + " mikrofon=\(microphone.rawValue) bildirimler=\(notifications.rawValue)")
        lines.append("girişte başlat: \(loginItemStatus)")
        lines.append("kısayol: \(hotKeyLabel)")
        lines.append("depolama kökü: \(fileStore.rootURL.path)")
        lines.append("veritabanı: \(Self.fileSizeText(at: fileStore.databaseURL))")
        lines.append("boş disk alanı: \(Self.freeSpaceText(at: fileStore.rootURL))")
        lines.append("transkripsiyon modeli: \(Self.describe(transcriberState)) (\(settings.sttModel))")
        return lines.joined(separator: "\n") + "\n"
    }

    private func authSummary() async -> String {
        guard let claudeExecutable else { return "claude yok" }
        let result = await Self.run(claudeExecutable, ["auth", "status"])
        guard result.status == 0, let data = result.stdout.data(using: .utf8),
            let json = try? JSONSerialization.jsonObject(with: data) as? [String: Any]
        else {
            return "okunamadı (exit \(result.status))"
        }
        let loggedIn = json["loggedIn"] as? Bool ?? false
        let method = json["authMethod"] as? String ?? "?"
        let subscription = json["subscriptionType"] as? String ?? "?"
        return "loggedIn=\(loggedIn) authMethod=\(method) subscriptionType=\(subscription)"
    }

    /// `codesign -dv` writes to stderr; the two interesting lines are `Authority=` and `flags=`.
    /// Spec §10.2: `Authority=Shotcue Dev` is what keeps TCC grants across rebuilds.
    private func signatureSummary() async -> String {
        let result = await Self.run(
            URL(fileURLWithPath: "/usr/bin/codesign"), ["-dv", Bundle.main.bundleURL.path])
        let lines = (result.stderr + result.stdout)
            .components(separatedBy: .newlines)
            .filter { $0.hasPrefix("Authority=") || $0.contains("flags=") }
        return lines.isEmpty ? "imza okunamadı" : lines.joined(separator: "; ")
    }

    private static func describe(_ state: TranscriberModelState) -> String {
        switch state {
        case .notDownloaded: return "indirilmedi"
        case .downloading(let progress): return "indiriliyor (%\(Int(progress * 100)))"
        case .ready: return "hazır"
        case .failed(let message): return "hata: \(message)"
        }
    }

    private static func fileSizeText(at url: URL) -> String {
        guard let values = try? url.resourceValues(forKeys: [.fileSizeKey]), let size = values.fileSize
        else { return "yok" }
        return ByteCountFormatter.string(fromByteCount: Int64(size), countStyle: .file)
    }

    private static func freeSpaceText(at url: URL) -> String {
        guard
            let values = try? url.resourceValues(forKeys: [.volumeAvailableCapacityForImportantUsageKey]),
            let capacity = values.volumeAvailableCapacityForImportantUsage
        else { return "bilinmiyor" }
        return ByteCountFormatter.string(fromByteCount: capacity, countStyle: .file)
    }

    /// Runs a short-lived tool off the main actor. `HOME`/`PATH` are set explicitly and
    /// `ANTHROPIC_API_KEY` is removed so `claude` reports the subscription session, not an API key
    /// (spec §6.4).
    static func run(
        _ executable: URL, _ arguments: [String], timeout: TimeInterval = 20
    ) async -> ProcessOutput {
        await Task.detached(priority: .utility) {
            runBlocking(executable, arguments, timeout: timeout)
        }.value
    }

    nonisolated private static func runBlocking(
        _ executable: URL, _ arguments: [String], timeout: TimeInterval
    ) -> ProcessOutput {
        let process = Process()
        process.executableURL = executable
        process.arguments = arguments
        var environment = ProcessInfo.processInfo.environment
        environment["HOME"] = NSHomeDirectory()
        environment["PATH"] =
            "\(NSHomeDirectory())/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
        environment.removeValue(forKey: "ANTHROPIC_API_KEY")
        process.environment = environment
        let out = Pipe()
        let err = Pipe()
        process.standardOutput = out
        process.standardError = err
        do {
            try process.run()
        } catch {
            return ProcessOutput(status: -1, stdout: "", stderr: "\(error)", timedOut: false)
        }
        let deadline = Date().addingTimeInterval(timeout)
        var timedOut = false
        while process.isRunning {
            if Date() >= deadline {
                timedOut = true
                process.terminate()
                break
            }
            usleep(50_000)
        }
        let stdout = String(data: out.fileHandleForReading.readDataToEndOfFile(), encoding: .utf8) ?? ""
        let stderr = String(data: err.fileHandleForReading.readDataToEndOfFile(), encoding: .utf8) ?? ""
        process.waitUntilExit()
        return ProcessOutput(
            status: process.terminationStatus, stdout: stdout, stderr: stderr, timedOut: timedOut)
    }
}
```

- [ ] **Step 3: Derle**

Run: `make build 2>&1 | tail -4`
Expected: `Build complete!`.

- [ ] **Step 4: Ayarlar'da claude sürümünü ve login item durumunu gör**

Run: `make run && sleep 3` → menü çubuğu > Ayarlar.
Expected: Ayarlar > Claude'da sürüm satırı `2.1.278 (Claude Code)` (veya makinedeki sürüm) ve **yeşil/normal** renkte; alt şeritte `claude: 2.1.278 (Claude Code)` ve `Girişte başlat: kapalı` yazar.

Ayarlar'da "Girişte başlat"ı aç, Ayarlar'ı kapat, tekrar aç.
Expected: Alt şeritte `Girişte başlat: etkin` **veya** `Sistem Ayarları > Genel > Giriş Öğeleri'nde onay bekliyor` yazar (ikisi de geçerli; macOS bazı durumlarda onay ister). Sistem Ayarları > Genel > Giriş Öğeleri'nde "Shotcue" satırı görünür.

Run (kanıt): `osascript -e 'tell application "System Events" to get the name of every login item' 2>/dev/null; sfltool dumpbtm 2>/dev/null | grep -A2 -i shotcue | head -10`
(`sfltool` root ister; alternatif kanıt Sistem Ayarları'ndaki satırın ekran görüntüsüdür.)

Kapat: toggle'ı kapat, Ayarlar'ı kapat, tekrar aç → `kapalı`.

- [ ] **Step 5: Tanılama raporunu gör**

Ayarlar'ın alt şeridinde "Tanılama çalıştır"a bas.
Expected: 1–3 saniye içinde `/tmp/shotcue-diagnostics.txt` varsayılan metin düzenleyicide açılır.

Run: `cat /tmp/shotcue-diagnostics.txt`
Expected (biçim birebir, değerler makineye göre):
```
Shotcue tanılama — 2026-09-22T19:41:05+03:00
app: /Users/egekibar/Applications/Shotcue.app
sürüm: 0.1.0 (1)
imza: Authority=Shotcue Dev; CodeDirectory v=20500 size=… flags=0x10000(runtime) hashes=…
claude yolu: /Users/egekibar/.local/bin/claude
claude ayarı: (otomatik)
claude sürümü: 2.1.278 (Claude Code)
claude auth: loggedIn=true authMethod=claude.ai subscriptionType=max
izinler: ekran kaydı=granted mikrofon=granted bildirimler=granted
girişte başlat: kapalı
kısayol: ⌃⇧2
depolama kökü: /Users/egekibar/Library/Application Support/Shotcue
veritabanı: 1,2 MB
boş disk alanı: 214,3 GB
transkripsiyon modeli: hazır (openai_whisper-large-v3-v20240930_turbo)
```
Kritik iki satır: `claude auth: loggedIn=true … subscriptionType=max` (spike S1'in kalıcı kanıtı — bundle'dan spawn edilen `claude` Keychain'i görüyor) ve `imza: Authority=Shotcue Dev` (spike S2). Raporda **e-posta veya org id bulunmaz**; bulunuyorsa `authSummary()` yanlış yazılmış, düzelt.

- [ ] **Step 6: `claude` yokken uygulamanın ayakta kaldığını gör**

Run:
```bash
make run && sleep 2
# Ayarlar > Claude > CLI yolu alanına gerçek olmayan bir yol yaz: /tmp/yok-boyle-bir-claude
# Ayarlar'ı kapat, uygulamayı yeniden başlat:
pkill -x Shotcue; sleep 1; open ~/Applications/Shotcue.app; sleep 3; pgrep -x Shotcue
```
Expected: Uygulama **açılır** (crash yok), menü çubuğu ikonu var; Ayarlar > Claude'da sürüm `bulunamadı` ve kırmızı; bir task göndermeye çalışınca run `failed` olur ve hata mesajı "`claude` komutu bulunamadı. Ayarlar > Claude'da yolu elle girin." (`MissingClaudeRunner`). Sonra yolu boşalt ve uygulamayı yeniden başlat → sürüm tekrar okunur.

- [ ] **Step 7: Commit**

```bash
xcrun swift-format format --in-place --recursive Sources/ShotcueApp
git add Sources/ShotcueApp/LoginItemManager.swift Sources/ShotcueApp/Diagnostics.swift
git commit -m "feat(app): add login item manager and diagnostics report

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: Uygulama ikonu (`scripts/render-icon.swift` + `scripts/make-icon.sh`)

**Files:**
- Create: `scripts/render-icon.swift`
- Create: `scripts/make-icon.sh`
- Create: `Resources/AppIcon.icns` (üretilen çıktı; repoya girer, 350–400 KB)
- Test: yok (doğrulama: `iconutil` çıktısı + Finder/Dock gözlemi)

**Interfaces:**
- Consumes: Plan 00'ın `Resources/Info.plist`'i (`CFBundleIconFile = AppIcon`) ve `scripts/bundle.sh`'i (`Resources/AppIcon.icns` varsa `Contents/Resources/` içine kopyalar). Bu task **`bundle.sh`'i ve `Info.plist`'i değiştirmez**; yalnızca eksik dosyayı üretir.
- Produces: `scripts/render-icon.swift` (1024×1024 PNG), `scripts/make-icon.sh` (`.iconset` → `.icns`), `Resources/AppIcon.icns`.

`.xcassets`/`actool` kullanılmaz (Global Constraints). Zincir: CoreGraphics çizimi → PNG → `sips` ile 10 boyut → `iconutil -c icns`.

- [ ] **Step 1: `scripts/render-icon.swift`'i yaz**

```swift
// Renders Shotcue's 1024x1024 app icon with CoreGraphics and writes it as a PNG.
// No .xcassets, no actool: `swift scripts/render-icon.swift out.png` runs in interpreter mode.
// Glyph: rounded-rect plate with a blue→indigo gradient, a camera viewfinder (four corner brackets)
// and a cue mark (ring + amber dot) in the middle.
import CoreGraphics
import Foundation
import ImageIO
import UniformTypeIdentifiers

let side = 1024
guard CommandLine.arguments.count == 2 else {
    FileHandle.standardError.write(Data("usage: render-icon.swift <output.png>\n".utf8))
    exit(2)
}
let output = URL(fileURLWithPath: CommandLine.arguments[1])

guard
    let context = CGContext(
        data: nil, width: side, height: side, bitsPerComponent: 8, bytesPerRow: 0,
        space: CGColorSpace(name: CGColorSpace.sRGB)!,
        bitmapInfo: CGImageAlphaInfo.premultipliedLast.rawValue)
else {
    FileHandle.standardError.write(Data("cannot create bitmap context\n".utf8))
    exit(1)
}

func color(_ r: Double, _ g: Double, _ b: Double, _ a: Double = 1) -> CGColor {
    CGColor(srgbRed: r, green: g, blue: b, alpha: a)
}

let full = CGRect(x: 0, y: 0, width: Double(side), height: Double(side))

// 1. Rounded-rect plate. macOS icons leave a margin inside the 1024 pt canvas; 100 pt reads well
//    at 16 pt and still fills the Dock tile.
let plate = full.insetBy(dx: 100, dy: 100)
let platePath = CGPath(roundedRect: plate, cornerWidth: 190, cornerHeight: 190, transform: nil)
context.saveGState()
context.addPath(platePath)
context.clip()
let gradient = CGGradient(
    colorsSpace: CGColorSpace(name: CGColorSpace.sRGB)!,
    colors: [color(0.29, 0.44, 0.96), color(0.36, 0.22, 0.80)] as CFArray,
    locations: [0, 1])!
context.drawLinearGradient(
    gradient,
    start: CGPoint(x: plate.midX, y: plate.maxY),
    end: CGPoint(x: plate.midX, y: plate.minY),
    options: [])
context.restoreGState()

// 2. Camera viewfinder: four corner brackets.
let viewfinder = plate.insetBy(dx: 150, dy: 150)
let armLength = 150.0
context.setStrokeColor(color(1, 1, 1, 0.96))
context.setLineWidth(46)
context.setLineCap(.round)
context.setLineJoin(.round)

func bracket(_ corner: CGPoint, dx: Double, dy: Double) {
    context.beginPath()
    context.move(to: CGPoint(x: corner.x + dx * armLength, y: corner.y))
    context.addLine(to: corner)
    context.addLine(to: CGPoint(x: corner.x, y: corner.y + dy * armLength))
    context.strokePath()
}

bracket(CGPoint(x: viewfinder.minX, y: viewfinder.maxY), dx: 1, dy: -1)
bracket(CGPoint(x: viewfinder.maxX, y: viewfinder.maxY), dx: -1, dy: -1)
bracket(CGPoint(x: viewfinder.minX, y: viewfinder.minY), dx: 1, dy: 1)
bracket(CGPoint(x: viewfinder.maxX, y: viewfinder.minY), dx: -1, dy: 1)

// 3. Cue mark: ring plus amber dot.
let center = CGPoint(x: viewfinder.midX, y: viewfinder.midY)
context.setStrokeColor(color(1, 1, 1, 0.55))
context.setLineWidth(26)
context.beginPath()
context.addArc(center: center, radius: 128, startAngle: 0, endAngle: .pi * 2, clockwise: false)
context.strokePath()

context.setFillColor(color(1, 0.86, 0.32, 1))
context.beginPath()
context.addArc(center: center, radius: 62, startAngle: 0, endAngle: .pi * 2, clockwise: false)
context.fillPath()

guard let image = context.makeImage() else {
    FileHandle.standardError.write(Data("cannot snapshot context\n".utf8))
    exit(1)
}
guard
    let destination = CGImageDestinationCreateWithURL(
        output as CFURL, UTType.png.identifier as CFString, 1, nil)
else {
    FileHandle.standardError.write(Data("cannot create PNG destination\n".utf8))
    exit(1)
}
CGImageDestinationAddImage(destination, image, nil)
guard CGImageDestinationFinalize(destination) else {
    FileHandle.standardError.write(Data("cannot write PNG\n".utf8))
    exit(1)
}
print("wrote \(output.path) (\(side)x\(side))")
```

- [ ] **Step 2: `scripts/make-icon.sh`'i yaz ve çalıştırılabilir yap**

```bash
#!/bin/bash
# Builds Resources/AppIcon.icns from the CoreGraphics-drawn 1024x1024 master PNG.
# Usage: scripts/make-icon.sh              (renders the glyph, then packs the .icns)
#        scripts/make-icon.sh master.png   (packs an existing 1024x1024 PNG instead)
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
WORK="$(mktemp -d)"
trap 'rm -rf "$WORK"' EXIT
MASTER="${1:-}"

if [ -z "$MASTER" ]; then
  MASTER="$WORK/AppIcon-1024.png"
  swift "$ROOT/scripts/render-icon.swift" "$MASTER"
fi
[ -f "$MASTER" ] || { echo "master PNG not found: $MASTER" >&2; exit 1; }
W="$(sips -g pixelWidth "$MASTER" | awk '/pixelWidth/{print $2}')"
H="$(sips -g pixelHeight "$MASTER" | awk '/pixelHeight/{print $2}')"
[ "$W" = "1024" ] && [ "$H" = "1024" ] || { echo "master must be 1024x1024, got ${W}x${H}" >&2; exit 1; }

SET="$WORK/AppIcon.iconset"
mkdir -p "$SET"
# name:pixels — iconutil requires exactly these file names for a complete macOS icon.
for pair in \
  icon_16x16:16 icon_16x16@2x:32 \
  icon_32x32:32 icon_32x32@2x:64 \
  icon_128x128:128 icon_128x128@2x:256 \
  icon_256x256:256 icon_256x256@2x:512 \
  icon_512x512:512 icon_512x512@2x:1024
do
  name="${pair%%:*}"; px="${pair##*:}"
  sips -s format png -z "$px" "$px" "$MASTER" --out "$SET/$name.png" >/dev/null
done

mkdir -p "$ROOT/Resources"
iconutil -c icns "$SET" -o "$ROOT/Resources/AppIcon.icns"
echo "Wrote $ROOT/Resources/AppIcon.icns ($(du -h "$ROOT/Resources/AppIcon.icns" | cut -f1))"
```

Run: `chmod +x scripts/make-icon.sh`

- [ ] **Step 3: İkonu üret ve doğrula**

Run:
```bash
./scripts/make-icon.sh
file Resources/AppIcon.icns
sips -g format Resources/AppIcon.icns
```
Expected:
```
wrote /var/folders/…/AppIcon-1024.png (1024x1024)
Wrote …/Resources/AppIcon.icns (364K)
Resources/AppIcon.icns: Mac OS X icon, … bytes, "ic12" type
  format: icns
```
Boyut ~300–420 KB aralığında olmalı; çok küçükse (< 50 KB) `sips` adımlarından biri sessizce atlanmıştır, `ls -la "$WORK/AppIcon.iconset"` ile 10 PNG'nin varlığını kontrol et (script `trap` ile sildiği için geçici olarak `trap` satırını yorumlayarak bak).

- [ ] **Step 4: Bundle'da ikonun göründüğünü gör**

Run:
```bash
make install
ls -la ~/Applications/Shotcue.app/Contents/Resources/AppIcon.icns
open -R ~/Applications/Shotcue.app
```
Expected: `AppIcon.icns` bundle içinde; Finder'da Shotcue.app **mavi-mor kare, beyaz köşe ayraçları ve ortada amber nokta** olarak görünür (kağıt-üstünde-jenerik ikon değil). Finder ikon önbelleği eski ikonu gösteriyorsa `killall Finder` ile yenile.

Run: `make run && sleep 2` → `SHOTCUE_OPEN_LIBRARY=1` ile kütüphaneyi aç.
Expected: Dock'ta (uygulama `.regular` olduğunda) aynı ikon görünür.

- [ ] **Step 5: Küçük boyutta okunabilirliği gör**

Run: `./scripts/shot.sh Shotcue /tmp/shotcue-shots >/dev/null; sips -s format png -z 32 32 Resources/AppIcon.icns --out /tmp/shotcue-icon-32.png && open /tmp/shotcue-icon-32.png`
Expected: 32 px'te ayraçlar ve orta nokta hâlâ ayırt edilebilir (menü çubuğu ikonu SF Symbol olduğu için bu yalnızca Dock/Finder okunabilirliğidir). Bulanık bir lekeye dönüşüyorsa `render-icon.swift` içindeki `setLineWidth(46)` değerini 56'ya çıkar ve Step 3–4'ü yinele.

- [ ] **Step 6: Commit**

```bash
git add scripts/render-icon.swift scripts/make-icon.sh Resources/AppIcon.icns
git commit -m "feat(app): generate app icon with CoreGraphics and iconutil

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 9: Uçtan uca manuel doğrulama (spec §11 kontrol listesi)

**Files:**
- Create: `docs/superpowers/plans/verification/README.md` (gözlem tablosu: her satır için tarih, sonuç, kanıt yolu)
- Create: `docs/superpowers/plans/verification/*.png` (`scripts/shot.sh` çıktıları; yalnızca yollar planda geçer, görüntüler bu task'ta üretilir)
- Test: yok — bu task'ın tamamı doğrulamadır

**Interfaces:**
- Consumes: Task 1–8'in ürettiği uygulama; `make`, `scripts/shot.sh`, `scripts/bundle.sh`, `codesign`, `sqlite3`, `pmset`, gerçek `claude` (2.1.278) ve gerçek bir git repo'su.
- Produces: `docs/superpowers/plans/verification/README.md` içindeki doldurulmuş tablo ve ekran görüntüsü dosyaları. Bir satır "başarısız" ise Plan 06 **tamamlanmış sayılmaz**; bulgu ilgili task'a geri döner.

Kurulum (tüm adımlar öncesi bir kez):
```bash
make cert          # sertifika yoksa; Keychain Access'te "Always Trust" kullanıcı adımı
make install
mkdir -p docs/superpowers/plans/verification
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite ".tables"    # şema hazır mı
```
Test projesi: gerçek, temiz bir git repo'su (ör. `~/Projects/shotcue-testbed`) oluştur, içine küçük bir dosya koy, commit et. Kütüphane > Projeler > "+" ile bu yolu ekle.

- [ ] **Step 1: V1 — İmza ve TCC kalıcılığı**

Run:
```bash
codesign -dv ~/Applications/Shotcue.app 2>&1 | grep -E "Authority|Identifier|flags"
touch Sources/ShotcueApp/AppDelegate.swift && make install
codesign -dv ~/Applications/Shotcue.app 2>&1 | grep -E "Authority|flags"
open ~/Applications/Shotcue.app && sleep 3
```
Expected: İki çağrıda da `Identifier=com.shotcue.app` ve **`Authority=Shotcue Dev`** satırı görünür; `flags` içinde `adhoc` **yok** (`flags=0x10000(runtime)`). Yeniden kurulumdan sonra `⌃⇧2` ile yakalama yaparken **yeni Ekran Kaydı izin diyaloğu çıkmaz**. `Authority` satırı yoksa imza ad-hoc kalmıştır → `make cert` ve Keychain güven adımı tekrarlanır (spec §10.2).

- [ ] **Step 2: V2 — İzin onboarding'i (sıfırdan)**

Run: `make reset-tcc && make run && sleep 3`
Expected: "Shotcue Kurulumu" penceresi açılır; Ekran Kaydı ve Mikrofon satırları "izin verilmedi" gösterir. Her iki "Sistem Ayarları'nda aç" düğmesi doğru paneli açar (Screen & System Audio Recording / Microphone). İzinleri ver, "Bitti" → uygulama kendini yeniden başlatır (yeni PID).

Run: `./scripts/shot.sh Shotcue docs/superpowers/plans/verification && mv docs/superpowers/plans/verification/window-1.png docs/superpowers/plans/verification/v2-onboarding.png`
Kanıt: `docs/superpowers/plans/verification/v2-onboarding.png`

- [ ] **Step 3: V3 — İki ekranda yakalama**

Harici ekran bağlı olmalı (yoksa satır "ekran yok — doğrulanamadı" olarak işaretlenir ve Plan 06 bu tek satırla açık kapanır).

Run: Fareyi **birincil** ekrana taşı, `⌃⇧2`, bir bölge seç. Sonra fareyi **ikincil** ekrana taşı, `⌃⇧2`, bir bölge seç.
Expected: Her iki durumda panel **farenin bulunduğu ekranın** sağ üst köşesinde (16 pt boşlukla, menü çubuğunun altında) açılır. Her iki PNG doğru piksel boyutunda kaydedilir ve `scale` alanı ekranın gerçek ölçeğini yansıtır (Retina'da 2, 1x harici ekranda 1):
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select width, height, scale from capture order by created_at desc limit 2;"
```

- [ ] **Step 4: V4 — Panel odağı ve kısayolları**

Run: TextEdit'i öne al, `⌃⇧2` ile yakala.
Expected (dört gözlem, hepsi yazılacak):
1. TextEdit ön uygulama olarak kalır (başlık çubuğu aktif), Dock'ta Shotcue ikonu belirmez.
2. Klavyeden yazılan metin **panelin not alanına** gider.
3. `⌘1`…`⌘9` proje seçiciyi değiştirir (Plan 05 davranışı).
4. `⌘↩` → panel kapanır, task `ready`, run yok. Yeni yakalama + `⌘⇧↩` → task `queued`/`running`, **bir** run satırı. `Esc` ile kapatılan yakalama → task `inbox`, `project_id` NULL.

Run: `./scripts/shot.sh Shotcue docs/superpowers/plans/verification` (panel açıkken) → panel PNG'sini `v4-quick-panel.png` olarak yeniden adlandır.
Kanıt: `docs/superpowers/plans/verification/v4-quick-panel.png`

- [ ] **Step 5: V5 — Sesli not ve transkripsiyon**

Ayarlar > Ses'te modelin durumu "hazır" olmalı; değilse "Modeli indir"e bas ve bitmesini bekle.

Run: `⌃⇧2` ile yakala → panelde mikrofon düğmesine bas → Türkçe, içinde en az bir İngilizce teknik terim geçen bir cümle söyle ("Login ekranındaki buton rengi yanlış, primary color'ı düzelt") → mikrofon düğmesine tekrar bas (veya `⌘⇧V`).
Expected: Kayıt sırasında süre ve seviye çubuğu hareket eder. Durdurunca `audio/<uuid>.m4a` oluşur, `voice_note.transcript_state` önce `pending`:
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select transcript_state, length(transcript), engine from voice_note order by created_at desc limit 1;"
```
Birkaç saniye sonra aynı sorgu `done` ve dolu transkript verir; panel/Inspector'da transkript metni görünür ve **teknik terimler bozulmamış**. Task başlığı transkriptin ilk cümlesinden türer (`title_edited_by_user = 0`).

Panel kapatılıp transkripsiyonun devam ettiğini de gör: yeni bir sesli not kaydet, durdurduktan **hemen sonra** `Esc` ile paneli kapat → transkript yine `done` olur ve kütüphanede görünür.

- [ ] **Step 6: V6 — Gönderme, canlı log, sonuç, bildirim, terminal aktarımı**

Run: Kütüphane'de test projesine atanmış bir task seç, mod "Uygula", "Şimdi gönder".
Expected (sırayla gözlemlenecek):
1. Task `queued` → `running`; Inspector'da **canlı log** akar (`Read …`, `Edit …` gibi satırlar; `runs/<runId>.jsonl` dosyası büyür: `wc -l ~/Library/Application\ Support/Shotcue/runs/*.jsonl`).
2. Bitişte task `done`, run satırında `num_turns`, `cost_usd`, `result_text` dolu:
   ```bash
   sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
     "select state, subtype, num_turns, cost_usd, git_head_before is not null, git_head_after is not null
        from run order by started_at desc limit 1;"
   ```
   → `succeeded|success|<n>|<usd>|1|1`.
3. Bildirim çıkar (başlık + tek satır özet + maliyet), aksiyonlar **Aç** / **Terminalde devam et**.
4. "Aç" → kütüphane açılır, o task seçili.
5. "Terminalde devam et" → terminal açılır ve `claude --resume <run.id>` çalışır, oturum geçmişi görünür.
6. Inspector > "Diff'i göster" → değişen dosyalar metin olarak görünür (`git -C <proje> status --porcelain` ile aynı dosyalar).

Ayrıca bir **Analiz** run'ı çalıştır: mod "Analiz" ile gönder → run `done`, `git status --porcelain` çıktısı **boş** (dosya değişmedi).

Run: `./scripts/shot.sh Shotcue docs/superpowers/plans/verification` (canlı log akarken) → `v6-live-log.png`
Kanıt: `docs/superpowers/plans/verification/v6-live-log.png`

- [ ] **Step 7: V7 — Desktop aktarımı**

Run: Inspector > "Desktop'ta aç" ve "Desktop composer'da aç".
Expected: Claude Desktop açılır; "Desktop'ta aç" oturumu Code sekmesine import eder (`claude://code/resume?session=…`); "Desktop composer'da aç" prompt'u composer'a doldurur ve **otomatik göndermez** (araştırma 04 §3.3). Görselin gerçekten iliştirilip iliştirilmediğini not et; iliştirilmiyorsa Plan 04'ün `composerRoute`'u `.coworkNew`'e çevrilmelidir (Varsayım A9 / spike S5) — bulguyu `verification/README.md`'ye yaz.

- [ ] **Step 8: V8 — Tek seferlik zamanlama**

Run: Bir task'ı Inspector'dan "Tarih-saat" ile **2 dakika sonrası**na zamanla.
Expected: `task.status = scheduled`, `scheduled_at` dolu. Zaman gelince (en geç 30 sn gecikmeyle, zamanlayıcı aralığı) task `queued` → `running` olur ve bitişte bildirim çıkar:
```bash
watch -n 10 "sqlite3 ~/Library/Application\\ Support/Shotcue/shotcue.sqlite \
  'select status, scheduled_at from task where scheduled_at is not null order by updated_at desc limit 1;'"
```
Tetiklendikten sonra `scheduled_at` temizlenmiş veya task `done` olmuş olmalı; **ikinci kez tetiklenmemeli** (2 dakika daha bekleyip run sayısının artmadığını doğrula).

- [ ] **Step 9: V9 — Günlük kuyruk**

Run: Kütüphane > proje ayarlarında "Günlük kuyruk"u aç ve saati **şimdiden 1 dakika sonrasına** ayarla. O projede en az iki `ready` task olsun.
Expected: Saat gelince her iki task kuyruğa girer (manuel sıraya göre), proje başına **aynı anda en fazla 1 run** çalışır (ikinci task birincisi bitince başlar):
```bash
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select status, count(*) from task group by status;
   select project_id, daily_last_fired_at from project;"
```
`daily_last_fired_at` bugüne set edilir. Uygulamayı kapatıp açmadan 5 dakika bekle → **aynı gün tekrar tetiklenmez**.

- [ ] **Step 10: V10 — Uyku/uyanma catch-up**

Run:
```bash
# Bir task'ı 3 dakika sonrasına zamanla, sonra makineyi uyut:
pmset sleepnow
# 4+ dakika sonra uyandır (kapağı aç / bir tuşa bas), sonra:
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select status from task order by updated_at desc limit 1; select count(*) from run;"
```
Expected: Uyanmadan sonra **30 saniye içinde** kaçırılan zamanlama bir kez çalışır (`SchedulerDriver`'ın `NSWorkspace.didWakeNotification` gözlemcisi); run sayısı **bir** artar, flood yok. `log show --last 10m --predicate 'process == "Shotcue"' | grep -i wake` ile ek kanıt alınabilir.

- [ ] **Step 11: V11 — Yarım kalan run kurtarma**

Run:
```bash
# Bir task gönder, run başlar başlamaz uygulamayı sertçe kapat:
pkill -9 -x Shotcue
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite "select state from run order by started_at desc limit 1;"
open ~/Applications/Shotcue.app && sleep 4
sqlite3 ~/Library/Application\ Support/Shotcue/shotcue.sqlite \
  "select state, error from run order by started_at desc limit 1;"
```
Expected: Kapatmadan hemen sonra `running`; yeniden açılıştan sonra `failed` ve `error` içinde `interrupted` geçer (spec §8). Kütüphanede o run için "Terminalde devam et" hâlâ çalışır (session id korunur).

- [ ] **Step 12: V12 — Menü çubuğu menüsü ve Dock politikası**

Run: Menü çubuğu ikonuna tıkla.
Expected: Son 5 task ve durumları, "Kuyruğu şimdi çalıştır", "Duraklat/Sürdür", "Kütüphane", "Ayarlar", "Çık". "Duraklat"a bas → yeni gönderim `queued`'da bekler, `running`'e geçmez; "Sürdür" → çalışmaya başlar.

Dock: Kütüphane kapalıyken Dock'ta ikon yok; açıkken var; kapatınca kaybolur ve uygulama çalışmaya devam eder (`pgrep -x Shotcue`).

Run: `./scripts/shot.sh Shotcue docs/superpowers/plans/verification` (menü açıkken kütüphane de açık olsun) → `v12-menu-and-library.png`
Kanıt: `docs/superpowers/plans/verification/v12-menu-and-library.png`

- [ ] **Step 13: V13 — Ayarlar, login item, tanılama**

Run: Ayarlar'ı aç; dört sekmeyi (Genel / Claude / Ses / İzinler) gez.
Expected: Kısayol seçicide hazır 6 kombinasyon (`⌃⇧2`, `⌃⇧3`, `⌃⇧4`, `⌘⇧2`, `⌥Space`, `⌃⌥Space`); birini seç → Ayarlar'ı kapatmadan **yeni kısayol çalışır** (eski çalışmaz). "Girişte başlat"ı aç/kapat → Ayarlar'ı kapat-aç, durum satırı gerçek `SMAppService` durumunu gösterir. "Tanılama çalıştır" → `/tmp/shotcue-diagnostics.txt` açılır ve `claude auth: loggedIn=true` + `imza: Authority=Shotcue Dev` satırlarını içerir.

Run: `cp /tmp/shotcue-diagnostics.txt docs/superpowers/plans/verification/v13-diagnostics.txt`
Kanıt: `docs/superpowers/plans/verification/v13-diagnostics.txt`, `docs/superpowers/plans/verification/v13-settings.png`

- [ ] **Step 14: V14 — Hata yolları (spec §8 tablosu)**

Her satır için gözlem yaz:

| Durum | Nasıl tetiklenir | Beklenen |
|---|---|---|
| Ekran kaydı izni yok | `make reset-tcc` + `⌃⇧2` | Onboarding açılır, seçim imleci çıkmaz |
| Mikrofon izni yok | Sistem Ayarları'ndan mikrofonu kapat | Panelde ses düğmesi devre dışı + açıklama; metin notu çalışır |
| `screencapture` hatası | Depolama kökünü salt-okunur bir yola ayarla | Bildirim + Ayarlar'da hata; **task oluşmaz** |
| `claude` bulunamadı | Ayarlar > Claude > yol = `/tmp/yok` | Kırmızı durum, gönderim `failed` + açıklayıcı mesaj |
| `error_max_turns` | Max tur = 1 | Run `failed`, otomatik yeniden denenmez, Inspector limit artırma önerir |
| Zaman aşımı | Zaman aşımı = 1 dk, uzun bir görev gönder | Run `failed(timeout)`, süreç SIGINT→SIGKILL ile kapanır |
| Proje klasörü yok | Proje yolunu var olmayan bir dizine değiştir | Run başlamaz, proje kırmızı görünür |
| Whisper modeli yok | Ayarlar > Ses'te modeli sil / indirmeden dene | `transcript_state = failed`, ses saklanır, "tekrar dene" düğmesi |

- [ ] **Step 15: Gözlem tablosunu yaz**

`docs/superpowers/plans/verification/README.md`:
```markdown
# Plan 06 — uçtan uca doğrulama kayıtları

Makine: macOS 26.6.2 (25G83) · CLT 27.0 / Swift 6.4 · claude 2.1.278 · tarih: <doldur>

| # | Kontrol | Sonuç | Kanıt |
|---|---|---|---|
| V1 | İmza + TCC kalıcılığı (`Authority=Shotcue Dev`) | | |
| V2 | İzin onboarding'i + relaunch | | `v2-onboarding.png` |
| V3 | İki ekranda yakalama + ölçek | | |
| V4 | Panel odağı, ⌘↩ / ⌘⇧↩ / Esc | | `v4-quick-panel.png` |
| V5 | Sesli not → transkript | | |
| V6 | Gönder → canlı log → done → bildirim → terminal | | `v6-live-log.png` |
| V7 | Desktop resume + composer (görsel iliştiriliyor mu?) | | |
| V8 | Tek seferlik zamanlama (tek tetikleme) | | |
| V9 | Günlük kuyruk (proje başına 1 run, gün içinde tekrar yok) | | |
| V10 | Uyku/uyanma catch-up | | |
| V11 | Yarım kalan run → failed(interrupted) | | |
| V12 | Menü çubuğu menüsü + Dock politikası | | `v12-menu-and-library.png` |
| V13 | Ayarlar, kısayol değişimi, login item, tanılama | | `v13-diagnostics.txt`, `v13-settings.png` |
| V14 | Hata yolları (spec §8, 8 satır) | | |

## Notlar
- <gözlemler, sapmalar, açık kalan satırlar>
```

Tabloyu doldur; her satır `geçti` / `geçmedi (neden)` / `doğrulanamadı (neden)` olmalı. Boş bırakma.

- [ ] **Step 16: Commit**

```bash
git add docs/superpowers/plans/verification
git commit -m "docs: record end-to-end verification results for the app layer

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## Plan 06 tamamlanma ölçütü

- `make build` yeşil; `make test` Plan 01–05'in testleriyle yeşil (Plan 06 test eklemez, mevcut testleri kırmaz): `swift test 2>&1 | tail -3` → `passed`.
- `Sources/ShotcueApp/` içinde 15 dosya var ve **hiçbiri** `@State`, `#Preview`, `NSApp.activate(ignoringOtherApps:)` veya `.xcassets` kullanmıyor:
  ```bash
  grep -rn "@State \|#Preview\|activate(ignoringOtherApps" Sources/ShotcueApp | wc -l   # → 0
  ```
- `Package.swift`, `Makefile`, `Resources/Info.plist`, `Resources/Shotcue.entitlements` ve `Sources/Shotcue{Core,Persistence,Capture,Notes,ClaudeBridge,UI}/` bu planda **değişmemiş**:
  ```bash
  git diff --name-only <plan-06-öncesi-sha>..HEAD | grep -vE "^(Sources/ShotcueApp/|Resources/AppIcon.icns|scripts/(make-icon.sh|render-icon.swift)|docs/)" | wc -l   # → 0
  ```
- `make run` → menü çubuğunda Shotcue ikonu; Dock'ta ikon yok; `SHOTCUE_OPEN_LIBRARY=1` ile kütüphane açılıyor ve Dock ikonu geliyor, kapanınca gidiyor.
- `codesign -dv ~/Applications/Shotcue.app` → `Identifier=com.shotcue.app` **ve** `Authority=Shotcue Dev` (ad-hoc değil); iki ardışık `make install` sonrası Ekran Kaydı izni korunuyor.
- `Resources/AppIcon.icns` üretilmiş (300–420 KB, `file` → `Mac OS X icon`) ve `~/Applications/Shotcue.app/Contents/Resources/` içinde; Finder ve Dock'ta görünüyor.
- `/tmp/shotcue-diagnostics.txt` `claude auth: loggedIn=true … subscriptionType=max` ve `imza: Authority=Shotcue Dev` satırlarını içeriyor; içinde e-posta/org id **yok**.
- `docs/superpowers/plans/verification/README.md` tablosunda V1–V14'ün **hepsi** doldurulmuş; `geçmedi` satırı yok. `doğrulanamadı` yalnızca V3 (ikinci ekran yok) ve V7 (Claude Desktop kurulu değil) için kabul edilir ve gerekçesi yazılıdır.
- Spec §5'in beş akışı gerçek `claude` ile en az bir kez uçtan uca koşmuş: yakalama (V3/V4), sesli not (V5), kütüphane+detay (V6), gönderme ve sonuç (V6, bir Analiz + bir Uygula run'ı), zamanlama (V8/V9/V10).
- Varsayımlar tablosundaki (A1–A16) her satır ya "sözleşmeyle uyuştu" ya da "şu dosyada şu satır düzeltildi" olarak `verification/README.md` > Notlar bölümüne yazılmış.
- Bundan sonrası: `superpowers:requesting-code-review` ile App katmanı incelemesi, ardından `superpowers:finishing-a-development-branch`.
